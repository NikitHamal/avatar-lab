// Generated by scripts/generate-standalone-engine.mjs. Do not edit.
export const standaloneEngineSource = "var AvatarProceduralEngine=(function(){var modules={\"/avatar/surfaces\":function(require,module,exports){\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.surfaceSampleAt = exports.surfaceNormalAt = exports.surfaceFrontSampleAt = exports.surfacePointAt = exports.cursorLayout = exports.surfaceProfilePoints = exports.surfaceLabels = exports.surfacePresets = void 0;\nexports.surfacePresets = {\n    sphere: { type: 'sphere', width: 240, height: 240, depth: 240, roundness: 1 },\n    mickey: { type: 'mickey', width: 220, height: 210, depth: 145, roundness: 1 },\n    cursor: { type: 'cursor', width: 175, height: 260, depth: 145, roundness: 0 },\n    cube: { type: 'cube', width: 245, height: 245, depth: 220, roundness: 0 },\n    capsule: { type: 'capsule', width: 205, height: 270, depth: 205, roundness: 1 },\n    cylinder: {\n        type: 'cylinder',\n        width: 235,\n        height: 250,\n        depth: 215,\n        roundness: 0.45,\n        morphRoundness: 0,\n    },\n    cone: {\n        type: 'cone',\n        width: 250,\n        height: 265,\n        depth: 225,\n        roundness: 0,\n        morphRoundness: 0,\n        tipRoundness: 0.55,\n        baseRoundness: 0.45,\n    },\n    diamond: { type: 'diamond', width: 235, height: 260, depth: 215, roundness: 0 },\n    torus: { type: 'torus', width: 220, height: 48, depth: 220, roundness: 1 },\n    star: { type: 'star', width: 180, height: 180, depth: 70, roundness: 0 },\n    cloud: { type: 'cloud', width: 250, height: 190, depth: 180, roundness: 1 },\n    book: { type: 'book', width: 210, height: 140, depth: 95, roundness: 0.5 },\n    hand: { type: 'hand', width: 220, height: 235, depth: 95, roundness: 0.72 },\n    egg: { type: 'egg', width: 215, height: 260, depth: 205, roundness: 1 },\n    bean: { type: 'bean', width: 235, height: 220, depth: 190, roundness: 1 },\n    heart: { type: 'heart', width: 235, height: 220, depth: 150, roundness: 0.8 },\n    droplet: { type: 'droplet', width: 210, height: 275, depth: 190, roundness: 0.9 },\n    pebble: { type: 'pebble', width: 250, height: 205, depth: 175, roundness: 1.25 },\n    pyramid: { type: 'pyramid', width: 235, height: 255, depth: 215, roundness: 0.2 },\n    flower: { type: 'flower', width: 185, height: 185, depth: 70, roundness: 0.75 },\n    disc: { type: 'disc', width: 230, height: 230, depth: 72, roundness: 1 },\n    hexagon: { type: 'hexagon', width: 240, height: 225, depth: 150, roundness: 0.18 },\n    shield: { type: 'shield', width: 225, height: 255, depth: 150, roundness: 0.7 },\n    clover: { type: 'clover', width: 235, height: 235, depth: 135, roundness: 0.85 },\n    gem: { type: 'gem', width: 235, height: 245, depth: 175, roundness: 0.12 },\n};\nexports.surfaceLabels = {\n    sphere: 'Sphère',\n    mickey: 'Mickey',\n    cursor: 'Curseur',\n    cube: 'Cube',\n    capsule: 'Capsule',\n    cylinder: 'Cylindre',\n    cone: 'Cône',\n    diamond: 'Diamant',\n    torus: 'Halo / Anneau',\n    star: 'Étoile',\n    cloud: 'Nuage',\n    book: 'Livre',\n    hand: 'Main / Patte',\n    egg: 'Œuf',\n    bean: 'Haricot',\n    heart: 'Cœur',\n    droplet: 'Goutte',\n    pebble: 'Galet',\n    pyramid: 'Pyramide',\n    flower: 'Fleur',\n    disc: 'Disque',\n    hexagon: 'Hexagone',\n    shield: 'Bouclier',\n    clover: 'Trèfle',\n    gem: 'Gemme',\n};\nconst polarProfile = (width, height, samples, radiusAt) => Array.from({ length: samples }, (_, index) => {\n    const angle = -Math.PI / 2 + (index / samples) * Math.PI * 2;\n    const radius = radiusAt(angle);\n    return [Math.cos(angle) * (width / 2) * radius, Math.sin(angle) * (height / 2) * radius];\n});\nconst normalizedProfile = (config, points) => points.map(([x, y]) => [x * (config.width / 2), y * (config.height / 2)]);\nconst surfaceProfilePoints = (config) => {\n    switch (config.type) {\n        case 'star':\n            return polarProfile(config.width, config.height, 10, angle => {\n                const point = Math.round(((angle + Math.PI / 2) / (Math.PI * 2)) * 10);\n                return point % 2 === 0 ? 1 : 0.44;\n            });\n        case 'flower':\n            return polarProfile(config.width, config.height, 96, angle => 0.7 + 0.3 * (0.5 + 0.5 * Math.cos(angle * 6)));\n        case 'cloud':\n            return polarProfile(config.width, config.height, 96, angle => {\n                const lobes = 0.9 + 0.11 * Math.cos(angle * 3 - 0.35) + 0.055 * Math.cos(angle * 5 + 0.8);\n                const bottomFlatten = Math.sin(angle) > 0 ? 0.94 : 1;\n                return lobes * bottomFlatten;\n            });\n        case 'heart': {\n            const raw = Array.from({ length: 96 }, (_, index) => {\n                const t = (index / 96) * Math.PI * 2;\n                const x = 16 * Math.sin(t) ** 3;\n                const y = -(13 * Math.cos(t) - 5 * Math.cos(2 * t) - 2 * Math.cos(3 * t) - Math.cos(4 * t));\n                return [x / 17, (y + 1) / 17];\n            });\n            return normalizedProfile(config, raw);\n        }\n        case 'hand':\n            return normalizedProfile(config, [\n                [-0.74, 0.62], [-0.9, 0.28], [-0.78, 0.02], [-0.58, 0.04],\n                [-0.67, -0.34], [-0.52, -0.72], [-0.31, -0.7], [-0.23, -0.3],\n                [-0.12, -0.86], [0.1, -0.94], [0.22, -0.37], [0.35, -0.82],\n                [0.56, -0.76], [0.5, -0.24], [0.76, -0.42], [0.92, -0.2],\n                [0.76, 0.2], [0.66, 0.56], [0.35, 0.88], [-0.12, 0.98],\n            ]);\n        case 'egg':\n            return polarProfile(config.width, config.height, 96, angle => 1 - 0.14 * Math.sin(angle));\n        case 'bean':\n            return Array.from({ length: 96 }, (_, index) => {\n                const angle = -Math.PI / 2 + (index / 96) * Math.PI * 2;\n                const y = Math.sin(angle);\n                const bend = 0.15 * (y * y - 0.35);\n                const waist = 0.9 + 0.1 * Math.cos(angle * 2);\n                return [(Math.cos(angle) * waist + bend) * config.width / 2, y * config.height / 2];\n            });\n        case 'droplet':\n            return normalizedProfile(config, [\n                [0, -1], [0.28, -0.68], [0.58, -0.28], [0.78, 0.18], [0.72, 0.55],\n                [0.48, 0.84], [0, 1], [-0.48, 0.84], [-0.72, 0.55], [-0.78, 0.18],\n                [-0.58, -0.28], [-0.28, -0.68],\n            ]);\n        case 'pebble':\n            return polarProfile(config.width, config.height, 96, angle => 0.94 + 0.035 * Math.cos(angle * 3) + 0.02 * Math.sin(angle * 5));\n        case 'pyramid':\n            return normalizedProfile(config, [\n                [0, -1],\n                [0.94, 0.84],\n                [-0.94, 0.84],\n            ]);\n        case 'book':\n            return normalizedProfile(config, [\n                [-1, -0.82], [-0.18, -0.94], [0, -0.82], [0.18, -0.94], [1, -0.82],\n                [1, 0.82], [0.18, 0.94], [0, 0.82], [-0.18, 0.94], [-1, 0.82],\n            ]);\n        case 'hexagon':\n            return polarProfile(config.width, config.height, 6, () => 1);\n        case 'shield':\n            return normalizedProfile(config, [\n                [-0.9, -0.72], [0, -0.96], [0.9, -0.72], [0.82, 0.05], [0.58, 0.52],\n                [0, 1], [-0.58, 0.52], [-0.82, 0.05],\n            ]);\n        case 'clover':\n            return polarProfile(config.width, config.height, 120, angle => 0.7 + 0.3 * (0.5 + 0.5 * Math.cos(angle * 4)));\n        case 'gem':\n            return normalizedProfile(config, [\n                [-0.78, -0.58], [-0.34, -0.96], [0.34, -0.96], [0.78, -0.58],\n                [0.96, 0.05], [0.54, 0.78], [0, 1], [-0.54, 0.78], [-0.96, 0.05],\n            ]);\n        default:\n            return null;\n    }\n};\nexports.surfaceProfilePoints = surfaceProfilePoints;\nconst signedPower = (value, exponent) => Math.sign(value) * Math.abs(value) ** exponent;\nconst superellipsoid = (longitude, latitude, width, height, depth, verticalExponent, horizontalExponent) => {\n    const latitudeCosine = signedPower(Math.cos(latitude), verticalExponent);\n    return [\n        (width / 2) * latitudeCosine * signedPower(Math.sin(longitude), horizontalExponent),\n        (height / 2) * signedPower(Math.sin(latitude), verticalExponent),\n        (depth / 2) * latitudeCosine * signedPower(Math.cos(longitude), horizontalExponent),\n    ];\n};\nconst capsule = (config, longitude, latitude) => {\n    const radiusX = config.width / 2;\n    const radiusZ = config.depth / 2;\n    const capRadius = Math.min(radiusX, config.height / 2);\n    const straightHalf = Math.max(0, (config.height - capRadius * 2) / 2);\n    const meridianLength = straightHalf * 2 + Math.PI * capRadius;\n    const distance = ((latitude + Math.PI / 2) / Math.PI) * meridianLength;\n    let radial = radiusX;\n    let y = 0;\n    if (distance < (Math.PI * capRadius) / 2) {\n        const angle = -Math.PI / 2 + distance / capRadius;\n        radial = radiusX * Math.cos(angle);\n        y = -straightHalf + capRadius * Math.sin(angle);\n    }\n    else if (distance <= (Math.PI * capRadius) / 2 + straightHalf * 2) {\n        y = -straightHalf + distance - (Math.PI * capRadius) / 2;\n    }\n    else {\n        const angle = (distance - (Math.PI * capRadius) / 2 - straightHalf * 2) / capRadius;\n        radial = radiusX * Math.cos(angle);\n        y = straightHalf + capRadius * Math.sin(angle);\n    }\n    const depthScale = radiusX ? radiusZ / radiusX : 1;\n    return [radial * Math.sin(longitude), y, radial * depthScale * Math.cos(longitude)];\n};\nconst clampRoundness = (roundness) => Math.max(0, Math.min(2, roundness ?? 0));\nconst diamondExponent = (config) => 1 + clampRoundness(config.roundness) / 2;\nconst MIN_CUBE_SURFACE_POWER = 0.04;\nconst cubeExponent = (config) => {\n    if (config.roundness <= 0)\n        return Infinity;\n    const surfacePower = MIN_CUBE_SURFACE_POWER + (clampRoundness(config.roundness) / 2) * (1 - MIN_CUBE_SURFACE_POWER);\n    return 2 / surfacePower;\n};\nconst lpSurface = (config, longitude, latitude, exponent) => {\n    const sphereX = Math.cos(latitude) * Math.sin(longitude);\n    const sphereY = Math.sin(latitude);\n    const sphereZ = Math.cos(latitude) * Math.cos(longitude);\n    const length = Number.isFinite(exponent)\n        ? (Math.abs(sphereX) ** exponent +\n            Math.abs(sphereY) ** exponent +\n            Math.abs(sphereZ) ** exponent) **\n            (1 / exponent) || 1\n        : Math.max(Math.abs(sphereX), Math.abs(sphereY), Math.abs(sphereZ)) || 1;\n    return [\n        (config.width / 2) * (sphereX / length),\n        (config.height / 2) * (sphereY / length),\n        (config.depth / 2) * (sphereZ / length),\n    ];\n};\nconst diamond = (config, longitude, latitude) => {\n    return lpSurface(config, longitude, latitude, diamondExponent(config));\n};\nconst cube = (config, longitude, latitude) => lpSurface(config, longitude, latitude, cubeExponent(config));\nconst MAX_CONE_TIP_FRACTION = 0.24;\nconst MAX_CONE_BASE_FRACTION = 0.2;\nconst MAX_CYLINDER_EDGE_FRACTION = 0.22;\nconst morphProgress = (config) => clampRoundness(config.morphRoundness) / 2;\nconst morphProfileToEllipsoid = (config, progress, profile) => {\n    const amount = morphProgress(config);\n    const clampedProgress = Math.max(0, Math.min(1, progress));\n    const ellipsoidRadius = Math.sin(clampedProgress * Math.PI);\n    const ellipsoidVerticalProgress = (1 - Math.cos(clampedProgress * Math.PI)) / 2;\n    return {\n        radiusScale: profile.radiusScale + (ellipsoidRadius - profile.radiusScale) * amount,\n        verticalProgress: profile.verticalProgress + (ellipsoidVerticalProgress - profile.verticalProgress) * amount,\n    };\n};\nconst cubic = (start, firstControl, secondControl, end, progress) => {\n    const inverse = 1 - progress;\n    return (inverse ** 3 * start +\n        3 * inverse * inverse * progress * firstControl +\n        3 * inverse * progress * progress * secondControl +\n        progress ** 3 * end);\n};\nconst coneRounding = (config) => ({\n    tipFraction: (config.tipRoundness ?? 0) * MAX_CONE_TIP_FRACTION,\n    baseFraction: (config.baseRoundness ?? 0) * MAX_CONE_BASE_FRACTION,\n});\nconst cylinderProfileAt = (config, progress) => {\n    const clampedProgress = Math.max(0, Math.min(1, progress));\n    const edgeFraction = config.roundness * MAX_CYLINDER_EDGE_FRACTION;\n    if (edgeFraction <= 0) {\n        return {\n            radiusScale: 1,\n            verticalProgress: (Math.sin((clampedProgress - 0.5) * Math.PI) + 1) / 2,\n        };\n    }\n    if (clampedProgress < edgeFraction) {\n        const angle = -Math.PI / 2 + (clampedProgress / edgeFraction) * (Math.PI / 2);\n        return {\n            radiusScale: 1 - edgeFraction + edgeFraction * Math.cos(angle),\n            verticalProgress: (edgeFraction + edgeFraction * Math.sin(angle)) / 2,\n        };\n    }\n    if (clampedProgress > 1 - edgeFraction) {\n        const angle = ((clampedProgress - (1 - edgeFraction)) / edgeFraction) * (Math.PI / 2);\n        return {\n            radiusScale: 1 - edgeFraction + edgeFraction * Math.cos(angle),\n            verticalProgress: 1 - edgeFraction / 2 + (edgeFraction * Math.sin(angle)) / 2,\n        };\n    }\n    const middleProgress = (clampedProgress - edgeFraction) / (1 - edgeFraction * 2);\n    return {\n        radiusScale: 1,\n        verticalProgress: edgeFraction / 2 + middleProgress * (1 - edgeFraction),\n    };\n};\nconst morphedCylinderProfileAt = (config, progress) => morphProfileToEllipsoid(config, progress, cylinderProfileAt(config, progress));\nconst radiusScaleAtVerticalProgress = (config, verticalProgress, profileAt) => {\n    const progress = Math.max(0, Math.min(1, verticalProgress));\n    let lower = 0;\n    let upper = 1;\n    for (let iteration = 0; iteration < 14; iteration += 1) {\n        const candidate = (lower + upper) / 2;\n        if (profileAt(config, candidate).verticalProgress < progress)\n            lower = candidate;\n        else\n            upper = candidate;\n    }\n    return profileAt(config, (lower + upper) / 2).radiusScale;\n};\nconst coneProfileAt = (config, progress) => {\n    const clampedProgress = Math.max(0, Math.min(1, progress));\n    const { tipFraction, baseFraction } = coneRounding(config);\n    if (baseFraction > 0 && clampedProgress < baseFraction) {\n        const curveProgress = clampedProgress / baseFraction;\n        return {\n            radiusScale: cubic(1 - baseFraction, 1, 1 - baseFraction / 2, 1 - baseFraction, curveProgress),\n            verticalProgress: cubic(0, 0, baseFraction / 2, baseFraction, curveProgress),\n        };\n    }\n    if (tipFraction > 0 && clampedProgress > 1 - tipFraction) {\n        const curveProgress = (clampedProgress - (1 - tipFraction)) / tipFraction;\n        return {\n            radiusScale: cubic(tipFraction, tipFraction / 2, tipFraction / 4, 0, curveProgress),\n            verticalProgress: cubic(1 - tipFraction, 1 - tipFraction / 2, 1, 1, curveProgress),\n        };\n    }\n    return {\n        radiusScale: 1 - clampedProgress,\n        verticalProgress: clampedProgress,\n    };\n};\nconst morphedConeProfileAt = (config, progress) => morphProfileToEllipsoid(config, progress, coneProfileAt(config, progress));\nconst cursorLayout = (config) => {\n    const coneHeight = config.height * 0.36;\n    const bodyHeight = config.height - coneHeight;\n    return {\n        coneApexY: -config.height / 2,\n        coneBaseY: -config.height / 2 + coneHeight,\n        bodyHeight,\n        bodyCenterY: config.height / 2 - bodyHeight / 2,\n        bodyWidth: config.width * 0.54,\n        bodyDepth: config.depth * 0.62,\n    };\n};\nexports.cursorLayout = cursorLayout;\nconst surfacePointAt = (config, longitude, latitude) => {\n    const { width, height, depth } = config;\n    switch (config.type) {\n        case 'sphere':\n        case 'mickey':\n            return superellipsoid(longitude, latitude, width, height, depth, 1, 1);\n        case 'cube':\n            return cube(config, longitude, latitude);\n        case 'cylinder': {\n            const progress = (latitude + Math.PI / 2) / Math.PI;\n            const profile = morphedCylinderProfileAt(config, progress);\n            return [\n                (width / 2) * profile.radiusScale * Math.sin(longitude),\n                -height / 2 + height * profile.verticalProgress,\n                (depth / 2) * profile.radiusScale * Math.cos(longitude),\n            ];\n        }\n        case 'cursor': {\n            const layout = (0, exports.cursorLayout)(config);\n            const progress = (latitude + Math.PI / 2) / Math.PI;\n            const bodyConfig = {\n                ...config,\n                width: layout.bodyWidth,\n                height: layout.bodyHeight,\n                depth: layout.bodyDepth,\n            };\n            const profile = cylinderProfileAt(bodyConfig, progress);\n            return [\n                (layout.bodyWidth / 2) * profile.radiusScale * Math.sin(longitude),\n                layout.bodyCenterY - layout.bodyHeight / 2 + layout.bodyHeight * profile.verticalProgress,\n                (layout.bodyDepth / 2) * profile.radiusScale * Math.cos(longitude),\n            ];\n        }\n        case 'diamond':\n            return diamond(config, longitude, latitude);\n        case 'capsule':\n            return capsule(config, longitude, latitude);\n        case 'cone': {\n            const progress = (latitude + Math.PI / 2) / Math.PI;\n            const profile = morphedConeProfileAt(config, progress);\n            return [\n                (width / 2) * profile.radiusScale * Math.sin(longitude),\n                height / 2 - height * profile.verticalProgress,\n                (depth / 2) * profile.radiusScale * Math.cos(longitude),\n            ];\n        }\n        case 'torus': {\n            const ringRadius = (width + depth) / 4;\n            const tubeRadius = Math.max(2, height / 2);\n            const u = longitude;\n            const v = latitude * 2;\n            const radial = ringRadius + tubeRadius * Math.cos(v);\n            return [radial * Math.sin(u), tubeRadius * Math.sin(v), radial * Math.cos(u)];\n        }\n        case 'star': {\n            const angle = longitude;\n            const modulation = (Math.abs(Math.sin(2 * angle)) + 0.3) / 1.3;\n            const latCos = Math.cos(latitude);\n            return [\n                (width / 2) * latCos * Math.sin(angle) * modulation,\n                (height / 2) * Math.sin(latitude) * modulation,\n                (depth / 2) * latCos * Math.cos(angle) * modulation,\n            ];\n        }\n        case 'cloud': {\n            const latCos = Math.cos(latitude);\n            const lobeMod = 1 + 0.18 * Math.cos(3 * longitude) * Math.cos(2 * latitude);\n            const vertMod = 1 + 0.15 * Math.sin(3 * longitude);\n            return [\n                (width / 2) * latCos * Math.sin(longitude) * lobeMod,\n                (height / 2) * Math.sin(latitude) * vertMod,\n                (depth / 2) * latCos * Math.cos(longitude) * lobeMod,\n            ];\n        }\n        case 'book': {\n            const side = Math.sin(longitude);\n            const latCos = Math.cos(latitude);\n            const openAngle = 0.28;\n            const x = (width / 2) * side;\n            const y = (height / 2) * Math.sin(latitude);\n            const z = (depth / 2) * latCos * Math.cos(longitude) + Math.abs(side) * (depth / 2) * openAngle;\n            return [x, y, z];\n        }\n        case 'hand': {\n            const latCos = Math.cos(latitude);\n            const gripMod = 1 + 0.15 * Math.cos(longitude);\n            return [\n                (width / 2) * latCos * Math.sin(longitude),\n                (height / 2) * Math.sin(latitude) * gripMod,\n                (depth / 2) * latCos * Math.cos(longitude),\n            ];\n        }\n        case 'egg': {\n            const latSin = Math.sin(latitude);\n            const latCos = Math.cos(latitude);\n            const taper = 1 - 0.17 * latSin;\n            return [\n                (width / 2) * latCos * Math.sin(longitude) * taper,\n                (height / 2) * latSin,\n                (depth / 2) * latCos * Math.cos(longitude) * taper,\n            ];\n        }\n        case 'bean': {\n            const latSin = Math.sin(latitude);\n            const latCos = Math.cos(latitude);\n            const waist = 0.88 + 0.14 * Math.cos(latitude * 2);\n            const bend = width * 0.08 * (latSin * latSin - 0.35);\n            return [\n                bend + (width / 2) * latCos * Math.sin(longitude) * waist,\n                (height / 2) * latSin,\n                (depth / 2) * latCos * Math.cos(longitude) * (1 - 0.08 * latSin),\n            ];\n        }\n        case 'heart': {\n            const latSin = Math.sin(latitude);\n            const latCos = Math.cos(latitude);\n            const front = Math.cos(longitude);\n            const side = Math.sin(longitude);\n            const upper = Math.max(0, -latSin);\n            const lobe = 1 + 0.18 * upper * Math.cos(longitude * 2);\n            const cleft = 1 - 0.22 * upper * Math.max(0, front);\n            const point = 1 - 0.18 * Math.max(0, latSin);\n            return [\n                (width / 2) * latCos * side * lobe,\n                (height / 2) * latSin * point + height * 0.035,\n                (depth / 2) * latCos * front * cleft,\n            ];\n        }\n        case 'droplet': {\n            const progress = (latitude + Math.PI / 2) / Math.PI;\n            const latSin = Math.sin(latitude);\n            const latCos = Math.cos(latitude);\n            const taper = Math.max(0.08, 1.14 - progress * 0.78);\n            return [\n                (width / 2) * latCos * Math.sin(longitude) * taper,\n                (height / 2) * latSin,\n                (depth / 2) * latCos * Math.cos(longitude) * taper,\n            ];\n        }\n        case 'pebble': {\n            const verticalExponent = 0.82 + clampRoundness(config.roundness) * 0.18;\n            const horizontalExponent = 0.82 + clampRoundness(config.roundness) * 0.2;\n            const point = superellipsoid(longitude, latitude, width, height, depth, verticalExponent, horizontalExponent);\n            const wobble = 1 + 0.035 * Math.cos(3 * longitude) * Math.cos(latitude);\n            return [point[0] * wobble, point[1], point[2] * wobble];\n        }\n        case 'pyramid': {\n            const progress = (latitude + Math.PI / 2) / Math.PI;\n            const y = height / 2 - progress * height;\n            const scale = Math.max(0.02, progress);\n            const angle = longitude + Math.PI / 4;\n            const squareRadius = 1 / Math.max(Math.abs(Math.cos(angle)), Math.abs(Math.sin(angle)), 0.001);\n            return [\n                (width / 2) * scale * Math.sin(longitude) * squareRadius * 0.72,\n                y,\n                (depth / 2) * scale * Math.cos(longitude) * squareRadius * 0.72,\n            ];\n        }\n        case 'flower': {\n            const latCos = Math.cos(latitude);\n            const petal = 0.7 + 0.3 * (0.5 + 0.5 * Math.cos(longitude * 5));\n            const dome = 0.82 + 0.18 * Math.cos(latitude * 2);\n            return [\n                (width / 2) * latCos * Math.sin(longitude) * petal,\n                (height / 2) * Math.sin(latitude) * petal,\n                (depth / 2) * latCos * Math.cos(longitude) * dome,\n            ];\n        }\n        case 'disc':\n            return superellipsoid(longitude, latitude, width, height, depth, 0.9, 0.9);\n        case 'hexagon':\n            return superellipsoid(longitude, latitude, width, height, depth, 0.72, 0.72);\n        case 'shield': {\n            const latSin = Math.sin(latitude);\n            const latCos = Math.cos(latitude);\n            const taper = 1 - 0.22 * Math.max(0, latSin);\n            return [\n                (width / 2) * latCos * Math.sin(longitude) * taper,\n                (height / 2) * latSin,\n                (depth / 2) * latCos * Math.cos(longitude) * taper,\n            ];\n        }\n        case 'clover': {\n            const latCos = Math.cos(latitude);\n            const petal = 0.76 + 0.24 * (0.5 + 0.5 * Math.cos(longitude * 4));\n            return [\n                (width / 2) * latCos * Math.sin(longitude) * petal,\n                (height / 2) * Math.sin(latitude) * petal,\n                (depth / 2) * latCos * Math.cos(longitude),\n            ];\n        }\n        case 'gem':\n            return lpSurface(config, longitude, latitude, 1.2);\n    }\n};\nexports.surfacePointAt = surfacePointAt;\nconst subtract = (left, right) => [\n    left[0] - right[0],\n    left[1] - right[1],\n    left[2] - right[2],\n];\nconst normalize = ([x, y, z]) => {\n    const length = Math.hypot(x, y, z) || 1;\n    return [x / length, y / length, z / length];\n};\nconst normalFromTangents = (config, longitudeTangent, latitudeTangent) => {\n    const orientation = config.type === 'cone' ? -1 : 1;\n    return normalize([\n        orientation *\n            (longitudeTangent[1] * latitudeTangent[2] - longitudeTangent[2] * latitudeTangent[1]),\n        orientation *\n            (longitudeTangent[2] * latitudeTangent[0] - longitudeTangent[0] * latitudeTangent[2]),\n        orientation *\n            (longitudeTangent[0] * latitudeTangent[1] - longitudeTangent[1] * latitudeTangent[0]),\n    ]);\n};\nconst tangentNormalAt = (config, longitude, latitude) => {\n    const epsilon = 0.0005;\n    if (config.type === 'cone' && latitude >= Math.PI / 2 - epsilon)\n        return [0, -1, 0];\n    const longitudeBefore = (0, exports.surfacePointAt)(config, longitude - epsilon, latitude);\n    const longitudeAfter = (0, exports.surfacePointAt)(config, longitude + epsilon, latitude);\n    const latitudeBefore = (0, exports.surfacePointAt)(config, longitude, Math.max(-Math.PI / 2, latitude - epsilon));\n    const latitudeAfter = (0, exports.surfacePointAt)(config, longitude, Math.min(Math.PI / 2, latitude + epsilon));\n    return normalFromTangents(config, subtract(longitudeAfter, longitudeBefore), subtract(latitudeAfter, latitudeBefore));\n};\nconst signedMagnitude = (value, exponent) => Math.sign(value) * Math.abs(value) ** exponent;\nconst lpNormal = (config, point, exponent) => {\n    const radiusX = config.width / 2 || 1;\n    const radiusY = config.height / 2 || 1;\n    const radiusZ = config.depth / 2 || 1;\n    return normalize([\n        signedMagnitude(point[0] / radiusX, exponent - 1) / radiusX,\n        signedMagnitude(point[1] / radiusY, exponent - 1) / radiusY,\n        signedMagnitude(point[2] / radiusZ, exponent - 1) / radiusZ,\n    ]);\n};\nconst diamondNormal = (config, point) => lpNormal(config, point, diamondExponent(config));\nconst cubeNormal = (config, point) => {\n    const exponent = cubeExponent(config);\n    if (Number.isFinite(exponent))\n        return lpNormal(config, point, exponent);\n    const normalized = [\n        point[0] / (config.width / 2 || 1),\n        point[1] / (config.height / 2 || 1),\n        point[2] / (config.depth / 2 || 1),\n    ];\n    const dominantAxis = normalized.reduce((largest, value, index) => (Math.abs(value) > Math.abs(normalized[largest]) ? index : largest), 0);\n    const normal = [\n        dominantAxis === 0 ? Math.sign(normalized[0]) : 0,\n        dominantAxis === 1 ? Math.sign(normalized[1]) : 0,\n        dominantAxis === 2 ? Math.sign(normalized[2]) : 0,\n    ];\n    return normal;\n};\nconst lpFrontSample = (config, x, y, exponent, normalAt) => {\n    const radiusX = config.width / 2 || 1;\n    const radiusY = config.height / 2 || 1;\n    const radiusZ = config.depth / 2 || 1;\n    if (!Number.isFinite(exponent)) {\n        const point = [\n            Math.max(-radiusX, Math.min(radiusX, x)),\n            Math.max(-radiusY, Math.min(radiusY, y)),\n            radiusZ,\n        ];\n        return { point, normal: normalAt(config, point) };\n    }\n    const normalizedY = Math.max(-1, Math.min(1, y / radiusY));\n    const availableX = Math.max(0, 1 - Math.abs(normalizedY) ** exponent) ** (1 / exponent);\n    const surfaceX = Math.max(-radiusX * availableX, Math.min(radiusX * availableX, x));\n    const normalizedX = surfaceX / radiusX;\n    const normalizedZ = Math.max(0, 1 - Math.abs(normalizedX) ** exponent - Math.abs(normalizedY) ** exponent) **\n        (1 / exponent);\n    const point = [surfaceX, normalizedY * radiusY, radiusZ * normalizedZ];\n    return { point, normal: normalAt(config, point) };\n};\nconst ellipsoidFrontSample = (x, y, radiusX, radiusY, radiusZ, centerY = 0) => {\n    const localY = y - centerY;\n    const remaining = Math.max(0, 1 - (x / (radiusX || 1)) ** 2 - (localY / (radiusY || 1)) ** 2);\n    const z = radiusZ * Math.sqrt(remaining);\n    return {\n        point: [x, y, z],\n        normal: normalize([\n            x / (radiusX * radiusX || 1),\n            localY / (radiusY * radiusY || 1),\n            z / (radiusZ * radiusZ || 1),\n        ]),\n    };\n};\nconst radialProfileFrontSample = (config, x, y, profileAt, verticalDirection) => {\n    const radiusX = config.width / 2 || 1;\n    const radiusZ = config.depth / 2 || 1;\n    const verticalProgress = Math.max(0, Math.min(1, 0.5 + verticalDirection * (y / config.height)));\n    const radialScale = radiusScaleAtVerticalProgress(config, verticalProgress, profileAt);\n    const sectionRadiusX = radiusX * radialScale;\n    const sectionRadiusZ = radiusZ * radialScale;\n    const surfaceX = Math.max(-sectionRadiusX, Math.min(sectionRadiusX, x));\n    const remaining = sectionRadiusX > 0 ? Math.max(0, 1 - (surfaceX / sectionRadiusX) ** 2) : 0;\n    const z = sectionRadiusZ * Math.sqrt(remaining);\n    const derivativeStep = 0.0001;\n    const previousProgress = Math.max(0, verticalProgress - derivativeStep);\n    const nextProgress = Math.min(1, verticalProgress + derivativeStep);\n    const previousScale = radiusScaleAtVerticalProgress(config, previousProgress, profileAt);\n    const nextScale = radiusScaleAtVerticalProgress(config, nextProgress, profileAt);\n    const scaleDerivative = (nextScale - previousScale) / (nextProgress - previousProgress || 1);\n    const radialRemainder = Math.max(Math.sqrt(remaining), 0.0001);\n    const depthRatio = radiusZ / radiusX;\n    const depthXDerivative = (-depthRatio * surfaceX) / (sectionRadiusX * radialRemainder || 1);\n    const depthYDerivative = (verticalDirection * radiusZ * scaleDerivative) / (config.height * radialRemainder || 1);\n    return {\n        point: [surfaceX, y, z],\n        normal: normalize([-depthXDerivative, -depthYDerivative, 1]),\n    };\n};\nconst surfaceFrontSampleAt = (config, x, y) => {\n    const radiusX = config.width / 2 || 1;\n    const radiusY = config.height / 2 || 1;\n    const radiusZ = config.depth / 2 || 1;\n    switch (config.type) {\n        case 'sphere':\n        case 'mickey':\n        case 'pebble':\n        case 'disc':\n            return ellipsoidFrontSample(x, y, radiusX, radiusY, radiusZ);\n        case 'cube':\n            return lpFrontSample(config, x, y, cubeExponent(config), cubeNormal);\n        case 'capsule': {\n            const capRadiusY = Math.min(radiusX, radiusY);\n            const straightHalf = Math.max(0, radiusY - capRadiusY);\n            const capCenterY = y < -straightHalf ? -straightHalf : y > straightHalf ? straightHalf : y;\n            return ellipsoidFrontSample(x, y, radiusX, capRadiusY, radiusZ, capCenterY);\n        }\n        case 'cylinder':\n            return radialProfileFrontSample(config, x, y, morphedCylinderProfileAt, 1);\n        case 'cursor': {\n            const layout = (0, exports.cursorLayout)(config);\n            const bodyConfig = {\n                ...config,\n                width: layout.bodyWidth,\n                height: layout.bodyHeight,\n                depth: layout.bodyDepth,\n            };\n            const sample = radialProfileFrontSample(bodyConfig, x, y - layout.bodyCenterY, cylinderProfileAt, 1);\n            return {\n                point: [sample.point[0], sample.point[1] + layout.bodyCenterY, sample.point[2]],\n                normal: sample.normal,\n            };\n        }\n        case 'cone':\n            return radialProfileFrontSample(config, x, y, morphedConeProfileAt, -1);\n        case 'diamond':\n            return lpFrontSample(config, x, y, diamondExponent(config), diamondNormal);\n        default:\n            return ellipsoidFrontSample(x, y, radiusX, radiusY, radiusZ);\n    }\n};\nexports.surfaceFrontSampleAt = surfaceFrontSampleAt;\nconst surfaceNormalAt = (config, longitude, latitude) => {\n    const point = (0, exports.surfacePointAt)(config, longitude, latitude);\n    if (config.type === 'sphere' ||\n        config.type === 'mickey' ||\n        config.type === 'pebble' ||\n        config.type === 'disc') {\n        const halfWidth = config.width / 2 || 1;\n        const halfHeight = config.height / 2 || 1;\n        const halfDepth = config.depth / 2 || 1;\n        return normalize([\n            point[0] / (halfWidth * halfWidth),\n            point[1] / (halfHeight * halfHeight),\n            point[2] / (halfDepth * halfDepth),\n        ]);\n    }\n    if (config.type === 'cylinder' && config.roundness <= 0 && (config.morphRoundness ?? 0) <= 0) {\n        return normalize([\n            Math.sin(longitude) / (config.width / 2 || 1),\n            0,\n            Math.cos(longitude) / (config.depth / 2 || 1),\n        ]);\n    }\n    if (config.type === 'diamond') {\n        return diamondNormal(config, point);\n    }\n    if (config.type === 'cube') {\n        return cubeNormal(config, point);\n    }\n    return tangentNormalAt(config, longitude, latitude);\n};\nexports.surfaceNormalAt = surfaceNormalAt;\nconst surfaceSampleAt = (config, longitude, latitude) => {\n    const point = (0, exports.surfacePointAt)(config, longitude, latitude);\n    if (config.type === 'sphere' ||\n        config.type === 'mickey' ||\n        config.type === 'pebble' ||\n        config.type === 'disc') {\n        const halfWidth = config.width / 2 || 1;\n        const halfHeight = config.height / 2 || 1;\n        const halfDepth = config.depth / 2 || 1;\n        return {\n            point,\n            normal: normalize([\n                point[0] / (halfWidth * halfWidth),\n                point[1] / (halfHeight * halfHeight),\n                point[2] / (halfDepth * halfDepth),\n            ]),\n        };\n    }\n    if (config.type === 'cylinder' && config.roundness <= 0 && (config.morphRoundness ?? 0) <= 0) {\n        return {\n            point,\n            normal: normalize([\n                Math.sin(longitude) / (config.width / 2 || 1),\n                0,\n                Math.cos(longitude) / (config.depth / 2 || 1),\n            ]),\n        };\n    }\n    if (config.type === 'diamond') {\n        return {\n            point,\n            normal: diamondNormal(config, point),\n        };\n    }\n    if (config.type === 'cube') {\n        return {\n            point,\n            normal: cubeNormal(config, point),\n        };\n    }\n    return {\n        point,\n        normal: tangentNormalAt(config, longitude, latitude),\n    };\n};\nexports.surfaceSampleAt = surfaceSampleAt;\n},\"/avatar/geometry\":function(require,module,exports){\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.renderAvatar = exports.renderEyeEditor = exports.interpolatePose = exports.poseFromExpression = exports.rotateBodyNodeAroundLocalAxis = exports.translateBodyNodeInCameraPlane = exports.translateBodyNodeAlongLocalAxis = exports.renderBodyNodeEditor = exports.rotationRing = exports.rotateExpressionWithArcball = exports.rotateExpressionAroundCamera = exports.rotateExpressionAroundAxis = exports.axisVector = exports.pupilContourPoints = exports.eyeContourPoints = exports.rotateWithQuaternion = exports.slerpQuaternion = exports.expressionWithOrientation = exports.quaternionToEuler = exports.quaternionFromVectors = exports.quaternionFromEuler = exports.quaternionFromAxisAngle = exports.multiplyQuaternions = exports.normalizeQuaternion = exports.radians = exports.clamp = exports.expressionFields = exports.RADIUS = void 0;\nconst surfaces_1 = require(\"./surfaces\");\nconst creatureExpression_1 = require(\"../creature/creatureExpression\");\nexports.RADIUS = 120;\nconst FOCAL_LENGTH = 620;\nconst QUARTER_ARC_SAMPLES = 14;\nexports.expressionFields = [\n    'headX',\n    'headY',\n    'headZ',\n    'widthLeft',\n    'widthRight',\n    'heightLeft',\n    'heightRight',\n    'spacing',\n    'positionXLeft',\n    'positionXRight',\n    'positionYLeft',\n    'positionYRight',\n    'leftAngle',\n    'rightAngle',\n    'perspective',\n];\nconst clamp = (value, min, max) => Math.max(min, Math.min(max, value));\nexports.clamp = clamp;\nconst radians = (degrees) => (degrees * Math.PI) / 180;\nexports.radians = radians;\nconst normalizeQuaternion = ([w, x, y, z]) => {\n    const length = Math.hypot(w, x, y, z) || 1;\n    return [w / length, x / length, y / length, z / length];\n};\nexports.normalizeQuaternion = normalizeQuaternion;\nconst multiplyQuaternions = ([aw, ax, ay, az], [bw, bx, by, bz]) => (0, exports.normalizeQuaternion)([\n    aw * bw - ax * bx - ay * by - az * bz,\n    aw * bx + ax * bw + ay * bz - az * by,\n    aw * by - ax * bz + ay * bw + az * bx,\n    aw * bz + ax * by - ay * bx + az * bw,\n]);\nexports.multiplyQuaternions = multiplyQuaternions;\nconst quaternionFromAxisAngle = ([x, y, z], angle) => {\n    const halfAngle = angle / 2;\n    const sine = Math.sin(halfAngle);\n    return (0, exports.normalizeQuaternion)([Math.cos(halfAngle), x * sine, y * sine, z * sine]);\n};\nexports.quaternionFromAxisAngle = quaternionFromAxisAngle;\nconst quaternionFromEuler = (x, y, z) => {\n    const xRotation = (0, exports.quaternionFromAxisAngle)([1, 0, 0], x);\n    const yRotation = (0, exports.quaternionFromAxisAngle)([0, 1, 0], y);\n    const zRotation = (0, exports.quaternionFromAxisAngle)([0, 0, 1], z);\n    return (0, exports.multiplyQuaternions)((0, exports.multiplyQuaternions)(zRotation, xRotation), yRotation);\n};\nexports.quaternionFromEuler = quaternionFromEuler;\nconst quaternionFromVectors = (from, to) => {\n    const dot = from[0] * to[0] + from[1] * to[1] + from[2] * to[2];\n    const cross = [\n        from[1] * to[2] - from[2] * to[1],\n        from[2] * to[0] - from[0] * to[2],\n        from[0] * to[1] - from[1] * to[0],\n    ];\n    return (0, exports.normalizeQuaternion)([1 + dot, cross[0], cross[1], cross[2]]);\n};\nexports.quaternionFromVectors = quaternionFromVectors;\nconst quaternionToEuler = ([w, x, y, z]) => {\n    const matrix00 = 1 - 2 * (y * y + z * z);\n    const matrix01 = 2 * (x * y - z * w);\n    const matrix10 = 2 * (x * y + z * w);\n    const matrix11 = 1 - 2 * (x * x + z * z);\n    const matrix20 = 2 * (x * z - y * w);\n    const matrix21 = 2 * (y * z + x * w);\n    const matrix22 = 1 - 2 * (x * x + y * y);\n    const headX = Math.asin((0, exports.clamp)(matrix21, -1, 1));\n    if (Math.abs(Math.cos(headX)) < 0.00001)\n        return [headX, 0, Math.atan2(matrix10, matrix00)];\n    return [headX, Math.atan2(-matrix20, matrix22), Math.atan2(-matrix01, matrix11)];\n};\nexports.quaternionToEuler = quaternionToEuler;\nconst nearestEquivalentAngle = (angle, current) => {\n    let result = angle;\n    while (result - current > 180)\n        result -= 360;\n    while (result - current < -180)\n        result += 360;\n    return (0, exports.clamp)(result, -365, 365);\n};\nconst expressionWithOrientation = (expression, orientation) => {\n    const [radiansX, radiansY, radiansZ] = (0, exports.quaternionToEuler)(orientation);\n    const x = (radiansX * 180) / Math.PI;\n    const y = (radiansY * 180) / Math.PI;\n    const z = (radiansZ * 180) / Math.PI;\n    return {\n        ...expression,\n        headX: nearestEquivalentAngle(x, expression.headX),\n        headY: nearestEquivalentAngle(y, expression.headY),\n        headZ: nearestEquivalentAngle(z, expression.headZ),\n    };\n};\nexports.expressionWithOrientation = expressionWithOrientation;\nconst slerpQuaternion = (start, end, progress) => {\n    let target = end;\n    let dot = start.reduce((total, value, index) => total + value * target[index], 0);\n    if (dot < 0) {\n        target = target.map(value => -value);\n        dot = -dot;\n    }\n    if (dot > 0.9995) {\n        return (0, exports.normalizeQuaternion)(start.map((value, index) => value + (target[index] - value) * progress));\n    }\n    const angle = Math.acos((0, exports.clamp)(dot, -1, 1));\n    const sine = Math.sin(angle);\n    const startWeight = Math.sin((1 - progress) * angle) / sine;\n    const targetWeight = Math.sin(progress * angle) / sine;\n    return (0, exports.normalizeQuaternion)(start.map((value, index) => value * startWeight + target[index] * targetWeight));\n};\nexports.slerpQuaternion = slerpQuaternion;\nconst rotateWithQuaternion = ([w, x, y, z], [px, py, pz]) => {\n    const tx = 2 * (y * pz - z * py);\n    const ty = 2 * (z * px - x * pz);\n    const tz = 2 * (x * py - y * px);\n    return [\n        px + w * tx + (y * tz - z * ty),\n        py + w * ty + (z * tx - x * tz),\n        pz + w * tz + (x * ty - y * tx),\n    ];\n};\nexports.rotateWithQuaternion = rotateWithQuaternion;\nconst roundedRectangle = (width, height) => {\n    const halfWidth = width / 2;\n    const halfHeight = height / 2;\n    const cornerRadius = Math.min(halfHeight, halfWidth);\n    const points = [];\n    const addLine = (start, end) => {\n        const samples = Math.max(2, Math.ceil(Math.hypot(end[0] - start[0], end[1] - start[1]) / 1.5));\n        for (let index = 0; index < samples; index += 1) {\n            const progress = index / samples;\n            points.push([\n                start[0] + (end[0] - start[0]) * progress,\n                start[1] + (end[1] - start[1]) * progress,\n            ]);\n        }\n    };\n    const addArc = (centerX, centerY, startAngle) => {\n        for (let index = 0; index < QUARTER_ARC_SAMPLES; index += 1) {\n            const angle = startAngle + (index / QUARTER_ARC_SAMPLES) * (Math.PI / 2);\n            points.push([\n                centerX + Math.cos(angle) * cornerRadius,\n                centerY + Math.sin(angle) * cornerRadius,\n            ]);\n        }\n    };\n    addLine([-halfWidth + cornerRadius, -halfHeight], [halfWidth - cornerRadius, -halfHeight]);\n    addArc(halfWidth - cornerRadius, -halfHeight + cornerRadius, -Math.PI / 2);\n    addLine([halfWidth, -halfHeight + cornerRadius], [halfWidth, halfHeight - cornerRadius]);\n    addArc(halfWidth - cornerRadius, halfHeight - cornerRadius, 0);\n    addLine([halfWidth - cornerRadius, halfHeight], [-halfWidth + cornerRadius, halfHeight]);\n    addArc(-halfWidth + cornerRadius, halfHeight - cornerRadius, Math.PI / 2);\n    addLine([-halfWidth, height / 2 - cornerRadius], [-halfWidth, -height / 2 + cornerRadius]);\n    addArc(-halfWidth + cornerRadius, -height / 2 + cornerRadius, Math.PI);\n    return points;\n};\nconst eyeContourPoints = (width, height, style = 'dot') => {\n    const halfWidth = width / 2;\n    const halfHeight = height / 2;\n    if (style === 'circle') {\n        const samples = 48;\n        const points = [];\n        for (let i = 0; i < samples; i++) {\n            const angle = (i / samples) * Math.PI * 2;\n            points.push([Math.cos(angle) * halfWidth, Math.sin(angle) * halfHeight]);\n        }\n        return points;\n    }\n    if (style === 'cat') {\n        const samplesPerSide = 24;\n        const points = [];\n        for (let i = 0; i <= samplesPerSide; i++) {\n            const t = i / samplesPerSide;\n            const y = -halfHeight + t * height;\n            const normY = y / (halfHeight || 1);\n            const profile = Math.pow(Math.max(0, 1 - normY * normY), 1.25);\n            points.push([halfWidth * profile, y]);\n        }\n        for (let i = 0; i <= samplesPerSide; i++) {\n            const t = i / samplesPerSide;\n            const y = halfHeight - t * height;\n            const normY = y / (halfHeight || 1);\n            const profile = Math.pow(Math.max(0, 1 - normY * normY), 1.25);\n            points.push([-halfWidth * profile, y]);\n        }\n        return points;\n    }\n    if (style === 'acorn') {\n        const points = [];\n        const domeSamples = 24;\n        for (let i = 0; i <= domeSamples; i++) {\n            const angle = Math.PI + (i / domeSamples) * Math.PI;\n            points.push([\n                Math.cos(angle) * halfWidth,\n                -halfHeight + (1 + Math.sin(angle)) * (halfHeight * 0.75),\n            ]);\n        }\n        const flankSamples = 16;\n        for (let i = 1; i <= flankSamples; i++) {\n            const t = i / flankSamples;\n            const y = -halfHeight + halfHeight * 0.75 + t * (height - halfHeight * 0.75);\n            const normT = (y - (-halfHeight + halfHeight * 0.75)) / (height - halfHeight * 0.75 || 1);\n            const x = halfWidth * Math.cos(normT * (Math.PI / 2));\n            points.push([x, y]);\n        }\n        for (let i = flankSamples - 1; i >= 1; i--) {\n            const t = i / flankSamples;\n            const y = -halfHeight + halfHeight * 0.75 + t * (height - halfHeight * 0.75);\n            const normT = (y - (-halfHeight + halfHeight * 0.75)) / (height - halfHeight * 0.75 || 1);\n            const x = -halfWidth * Math.cos(normT * (Math.PI / 2));\n            points.push([x, y]);\n        }\n        return points;\n    }\n    return roundedRectangle(width, height);\n};\nexports.eyeContourPoints = eyeContourPoints;\nconst pupilContourPoints = (width, height, style = 'dot') => {\n    const halfWidth = width / 2;\n    const halfHeight = height / 2;\n    if (style === 'cat') {\n        const slitWidth = halfWidth * 0.36;\n        const slitHeight = halfHeight * 0.88;\n        const samplesPerSide = 18;\n        const points = [];\n        for (let i = 0; i <= samplesPerSide; i++) {\n            const t = i / samplesPerSide;\n            const y = -slitHeight + t * (slitHeight * 2);\n            const normY = y / (slitHeight || 1);\n            const profile = Math.pow(Math.max(0, 1 - normY * normY), 1.35);\n            points.push([slitWidth * profile, y]);\n        }\n        for (let i = 0; i <= samplesPerSide; i++) {\n            const t = i / samplesPerSide;\n            const y = slitHeight - t * (slitHeight * 2);\n            const normY = y / (slitHeight || 1);\n            const profile = Math.pow(Math.max(0, 1 - normY * normY), 1.35);\n            points.push([-slitWidth * profile, y]);\n        }\n        return points;\n    }\n    if (style === 'circle') {\n        const pupilRadiusX = halfWidth * 0.48;\n        const pupilRadiusY = halfHeight * 0.48;\n        const samples = 32;\n        const points = [];\n        for (let i = 0; i < samples; i++) {\n            const angle = (i / samples) * Math.PI * 2;\n            points.push([Math.cos(angle) * pupilRadiusX, Math.sin(angle) * pupilRadiusY]);\n        }\n        return points;\n    }\n    if (style === 'acorn') {\n        const pupilWidth = halfWidth * 0.44;\n        const pupilHeight = halfHeight * 0.58;\n        const points = [];\n        const domeSamples = 16;\n        for (let i = 0; i <= domeSamples; i++) {\n            const angle = Math.PI + (i / domeSamples) * Math.PI;\n            points.push([\n                Math.cos(angle) * pupilWidth,\n                -pupilHeight + (1 + Math.sin(angle)) * (pupilHeight * 0.75),\n            ]);\n        }\n        const flankSamples = 12;\n        for (let i = 1; i <= flankSamples; i++) {\n            const t = i / flankSamples;\n            const y = -pupilHeight + pupilHeight * 0.75 + t * (pupilHeight * 2 - pupilHeight * 0.75);\n            const normT = (y - (-pupilHeight + pupilHeight * 0.75)) / (pupilHeight * 2 - pupilHeight * 0.75 || 1);\n            const x = pupilWidth * Math.cos(normT * (Math.PI / 2));\n            points.push([x, y]);\n        }\n        for (let i = flankSamples - 1; i >= 1; i--) {\n            const t = i / flankSamples;\n            const y = -pupilHeight + pupilHeight * 0.75 + t * (pupilHeight * 2 - pupilHeight * 0.75);\n            const normT = (y - (-pupilHeight + pupilHeight * 0.75)) / (pupilHeight * 2 - pupilHeight * 0.75 || 1);\n            const x = -pupilWidth * Math.cos(normT * (Math.PI / 2));\n            points.push([x, y]);\n        }\n        return points;\n    }\n    return roundedRectangle(width * 0.42, height * 0.62);\n};\nexports.pupilContourPoints = pupilContourPoints;\nconst project = (point, perspective) => {\n    const denominator = FOCAL_LENGTH - point[2] * perspective;\n    const scale = Math.abs(denominator) < 0.0001 ? FOCAL_LENGTH / 0.0001 : FOCAL_LENGTH / denominator;\n    return [point[0] * scale, point[1] * scale, point[2]];\n};\nconst axisVector = (axis) => axis === 'x' ? [1, 0, 0] : axis === 'y' ? [0, 1, 0] : [0, 0, 1];\nexports.axisVector = axisVector;\nconst rotateExpressionAroundAxis = (expression, axis, deltaDegrees) => {\n    const startOrientation = (0, exports.poseFromExpression)(expression).orientation;\n    const worldAxis = (0, exports.rotateWithQuaternion)(startOrientation, (0, exports.axisVector)(axis));\n    const orientation = (0, exports.multiplyQuaternions)((0, exports.quaternionFromAxisAngle)(worldAxis, (0, exports.radians)(deltaDegrees)), startOrientation);\n    return (0, exports.expressionWithOrientation)(expression, orientation);\n};\nexports.rotateExpressionAroundAxis = rotateExpressionAroundAxis;\nconst rotateExpressionAroundCamera = (expression, deltaRadians) => {\n    const startOrientation = (0, exports.poseFromExpression)(expression).orientation;\n    return (0, exports.expressionWithOrientation)(expression, (0, exports.multiplyQuaternions)((0, exports.quaternionFromAxisAngle)([0, 0, 1], deltaRadians), startOrientation));\n};\nexports.rotateExpressionAroundCamera = rotateExpressionAroundCamera;\nconst arcballVector = ([xValue, yValue]) => {\n    const x = xValue / exports.RADIUS;\n    const y = yValue / exports.RADIUS;\n    const squaredLength = x * x + y * y;\n    if (squaredLength <= 1)\n        return [x, y, Math.sqrt(1 - squaredLength)];\n    const length = Math.sqrt(squaredLength);\n    return [x / length, y / length, 0];\n};\nconst rotateExpressionWithArcball = (expression, startPoint, currentPoint) => {\n    const startOrientation = (0, exports.poseFromExpression)(expression).orientation;\n    const delta = (0, exports.quaternionFromVectors)(arcballVector(startPoint), arcballVector(currentPoint));\n    return (0, exports.expressionWithOrientation)(expression, (0, exports.multiplyQuaternions)(delta, startOrientation));\n};\nexports.rotateExpressionWithArcball = rotateExpressionWithArcball;\nconst rotationRing = (pose, axis, radius = 30) => Array.from({ length: 97 }, (_, index) => {\n    const angle = (index / 96) * Math.PI * 2;\n    const cosine = Math.cos(angle);\n    const sine = Math.sin(angle);\n    const point = axis === 'x' ? [0, cosine, sine] : axis === 'y' ? [cosine, 0, sine] : [cosine, sine, 0];\n    const rotated = (0, exports.rotateWithQuaternion)(pose.orientation, point);\n    return [rotated[0] * radius, rotated[1] * radius, rotated[2]];\n});\nexports.rotationRing = rotationRing;\nconst renderBodyNodeEditor = (pose, node, axisLength = 34, ringRadius = 26) => {\n    const projectInHeadSpace = (point) => project((0, exports.rotateWithQuaternion)(pose.orientation, point), pose.expression.perspective);\n    const center = projectInHeadSpace(node.position);\n    const localOrientation = (0, exports.quaternionFromEuler)((0, exports.radians)(node.rotation[0]), (0, exports.radians)(node.rotation[1]), (0, exports.radians)(node.rotation[2]));\n    const axes = Object.fromEntries(['x', 'y', 'z'].map(axis => {\n        const vector = (0, exports.rotateWithQuaternion)(localOrientation, (0, exports.axisVector)(axis));\n        return [\n            axis,\n            projectInHeadSpace([\n                node.position[0] + vector[0] * axisLength,\n                node.position[1] + vector[1] * axisLength,\n                node.position[2] + vector[2] * axisLength,\n            ]),\n        ];\n    }));\n    ['x', 'y', 'z'].forEach(axis => {\n        const endpoint = axes[axis];\n        if (Math.hypot(endpoint[0] - center[0], endpoint[1] - center[1]) >= 12)\n            return;\n        const fallback = axis === 'x'\n            ? [center[0] + 18, center[1], endpoint[2]]\n            : axis === 'y'\n                ? [center[0], center[1] + 18, endpoint[2]]\n                : [center[0] + 14, center[1] + 14, endpoint[2]];\n        axes[axis] = fallback;\n    });\n    const rings = Object.fromEntries(['x', 'y', 'z'].map(axis => [\n        axis,\n        Array.from({ length: 65 }, (_, index) => {\n            const angle = (index / 64) * Math.PI * 2;\n            const cosine = Math.cos(angle) * ringRadius;\n            const sine = Math.sin(angle) * ringRadius;\n            const localPoint = axis === 'x' ? [0, cosine, sine] : axis === 'y' ? [cosine, 0, sine] : [cosine, sine, 0];\n            const rotated = (0, exports.rotateWithQuaternion)(localOrientation, localPoint);\n            return projectInHeadSpace([\n                node.position[0] + rotated[0],\n                node.position[1] + rotated[1],\n                node.position[2] + rotated[2],\n            ]);\n        }),\n    ]));\n    return { center, axes, rings };\n};\nexports.renderBodyNodeEditor = renderBodyNodeEditor;\nconst translateBodyNodeAlongLocalAxis = (node, axis, distance) => {\n    const orientation = (0, exports.quaternionFromEuler)((0, exports.radians)(node.rotation[0]), (0, exports.radians)(node.rotation[1]), (0, exports.radians)(node.rotation[2]));\n    const direction = (0, exports.rotateWithQuaternion)(orientation, (0, exports.axisVector)(axis));\n    return {\n        ...node,\n        position: [\n            node.position[0] + direction[0] * distance,\n            node.position[1] + direction[1] * distance,\n            node.position[2] + direction[2] * distance,\n        ],\n    };\n};\nexports.translateBodyNodeAlongLocalAxis = translateBodyNodeAlongLocalAxis;\nconst translateBodyNodeInCameraPlane = (node, pose, screenDeltaX, screenDeltaY) => {\n    const cameraPosition = (0, exports.rotateWithQuaternion)(pose.orientation, node.position);\n    const denominator = FOCAL_LENGTH - cameraPosition[2] * pose.expression.perspective;\n    const perspectiveScale = Math.abs(denominator) < 0.0001 ? FOCAL_LENGTH / 0.0001 : FOCAL_LENGTH / denominator;\n    const [w, x, y, z] = pose.orientation;\n    const headDelta = (0, exports.rotateWithQuaternion)([w, -x, -y, -z], [screenDeltaX / perspectiveScale, screenDeltaY / perspectiveScale, 0]);\n    return {\n        ...node,\n        position: [\n            node.position[0] + headDelta[0],\n            node.position[1] + headDelta[1],\n            node.position[2] + headDelta[2],\n        ],\n    };\n};\nexports.translateBodyNodeInCameraPlane = translateBodyNodeInCameraPlane;\nconst rotateBodyNodeAroundLocalAxis = (node, axis, deltaDegrees) => {\n    const orientation = (0, exports.quaternionFromEuler)((0, exports.radians)(node.rotation[0]), (0, exports.radians)(node.rotation[1]), (0, exports.radians)(node.rotation[2]));\n    const rotated = (0, exports.multiplyQuaternions)(orientation, (0, exports.quaternionFromAxisAngle)((0, exports.axisVector)(axis), (0, exports.radians)(deltaDegrees)));\n    const next = (0, exports.quaternionToEuler)(rotated).map(value => (value * 180) / Math.PI);\n    return {\n        ...node,\n        rotation: next.map((value, index) => nearestEquivalentAngle(value, node.rotation[index])),\n    };\n};\nexports.rotateBodyNodeAroundLocalAxis = rotateBodyNodeAroundLocalAxis;\nconst path = (points, close = true) => {\n    if (!points.length)\n        return '';\n    return `M${points[0][0].toFixed(2)} ${points[0][1].toFixed(2)}${points\n        .slice(1)\n        .map(point => `L${point[0].toFixed(2)} ${point[1].toFixed(2)}`)\n        .join('')}${close ? 'Z' : ''}`;\n};\nconst poseFromExpression = (expression) => ({\n    expression,\n    orientation: (0, exports.quaternionFromEuler)((0, exports.radians)(expression.headX), (0, exports.radians)(expression.headY), (0, exports.radians)(expression.headZ)),\n});\nexports.poseFromExpression = poseFromExpression;\nconst interpolatePose = (from, to, progress) => {\n    const expression = { ...from.expression };\n    exports.expressionFields.forEach(field => {\n        expression[field] =\n            from.expression[field] + (to.expression[field] - from.expression[field]) * progress;\n    });\n    return {\n        expression,\n        orientation: (0, exports.slerpQuaternion)(from.orientation, to.orientation, progress),\n    };\n};\nexports.interpolatePose = interpolatePose;\nconst MAX_SURFACE_CACHE_ENTRIES = 24;\nconst HEAD_LATITUDE_SAMPLES = 25;\nconst HEAD_LONGITUDE_SAMPLES = 73;\nconst PRIMITIVE_RING_SAMPLES = 144;\nconst ROUNDED_PRIMITIVE_LATITUDE_SAMPLES = 33;\nconst ROUNDED_PRIMITIVE_LONGITUDE_SAMPLES = 73;\nconst headSamplesCache = new Map();\nconst accessorySamplesCache = new Map();\nconst wireSamplesCache = new Map();\nconst surfaceCacheKey = (surface) => [\n    surface.type,\n    surface.width,\n    surface.height,\n    surface.depth,\n    surface.roundness,\n    surface.morphRoundness,\n    surface.tipRoundness,\n    surface.baseRoundness,\n]\n    .map(value => (typeof value === 'number' ? value.toFixed(4) : value))\n    .join(':');\nconst cacheSurfaceValue = (cache, key, value) => {\n    if (cache.size >= MAX_SURFACE_CACHE_ENTRIES)\n        cache.delete(cache.keys().next().value);\n    cache.set(key, value);\n    return value;\n};\nconst localSurfacePoint = (surface, longitude, latitude) => (0, surfaces_1.surfaceSampleAt)(surface, longitude, latitude);\nconst projectLocalSurfacePoint = (pose, sample) => ({\n    point: project((0, exports.rotateWithQuaternion)(pose.orientation, sample.point), pose.expression.perspective),\n    normal: (0, exports.rotateWithQuaternion)(pose.orientation, sample.normal),\n});\nconst canonicalFaceCoordinates = (x, y) => {\n    const longitude = x / exports.RADIUS;\n    const latitude = y / exports.RADIUS;\n    return [exports.RADIUS * Math.cos(latitude) * Math.sin(longitude), exports.RADIUS * Math.sin(latitude)];\n};\nconst projectFacePoint = (pose, surface, x, y) => {\n    const [faceX, faceY] = canonicalFaceCoordinates(x, y);\n    return projectLocalSurfacePoint(pose, (0, surfaces_1.surfaceFrontSampleAt)(surface, faceX, faceY));\n};\nconst creatureEyeFrame = (pose, surface, blink, eyeOffset = { x: 0, y: 0 }) => {\n    const centerX = 0;\n    const centerY = -7;\n    const center = projectFacePoint(pose, surface, centerX, centerY);\n    const left = projectFacePoint(pose, surface, centerX - creatureExpression_1.CREATURE_FRAME_HALF_WIDTH, centerY);\n    const right = projectFacePoint(pose, surface, centerX + creatureExpression_1.CREATURE_FRAME_HALF_WIDTH, centerY);\n    const top = projectFacePoint(pose, surface, centerX, centerY - creatureExpression_1.CREATURE_FRAME_HALF_HEIGHT);\n    const bottom = projectFacePoint(pose, surface, centerX, centerY + creatureExpression_1.CREATURE_FRAME_HALF_HEIGHT);\n    return {\n        center: [center.point[0], center.point[1]],\n        xAxis: [(right.point[0] - left.point[0]) / 2, (right.point[1] - left.point[1]) / 2],\n        yAxis: [(bottom.point[0] - top.point[0]) / 2, (bottom.point[1] - top.point[1]) / 2],\n        visible: center.normal[2] > 0.08,\n        rig: (0, creatureExpression_1.creatureEyeRigFromExpression)(pose.expression, blink, eyeOffset),\n    };\n};\nconst eyePoints = (pose, surface, side, blink, offset = { x: 0, y: 0 }) => {\n    const expression = pose.expression;\n    const suffix = side < 0 ? 'Left' : 'Right';\n    const width = expression[`width${suffix}`];\n    const restingHeight = expression[`height${suffix}`];\n    const height = 5 + (restingHeight - 5) * blink;\n    const centerX = (side * expression.spacing) / 2 + expression[`positionX${suffix}`] + offset.x;\n    const centerY = expression[`positionY${suffix}`] + offset.y;\n    const angle = (0, exports.radians)(side < 0 ? expression.leftAngle : expression.rightAngle);\n    const eyeStyle = expression.eyeStyle || 'dot';\n    return (0, exports.eyeContourPoints)(width, height, eyeStyle).map(([localX, localY]) => {\n        const rotatedX = localX * Math.cos(angle) - localY * Math.sin(angle);\n        const rotatedY = localX * Math.sin(angle) + localY * Math.cos(angle);\n        return projectFacePoint(pose, surface, centerX + rotatedX, centerY + rotatedY);\n    });\n};\nconst pupilPoints = (pose, surface, side, blink, offset = { x: 0, y: 0 }) => {\n    const expression = pose.expression;\n    const suffix = side < 0 ? 'Left' : 'Right';\n    const width = expression[`width${suffix}`];\n    const restingHeight = expression[`height${suffix}`];\n    const height = 5 + (restingHeight - 5) * blink;\n    const centerX = (side * expression.spacing) / 2 + expression[`positionX${suffix}`] + offset.x;\n    const centerY = expression[`positionY${suffix}`] + offset.y;\n    const angle = (0, exports.radians)(side < 0 ? expression.leftAngle : expression.rightAngle);\n    const eyeStyle = expression.eyeStyle || 'dot';\n    return (0, exports.pupilContourPoints)(width, height, eyeStyle).map(([localX, localY]) => {\n        const rotatedX = localX * Math.cos(angle) - localY * Math.sin(angle);\n        const rotatedY = localX * Math.sin(angle) + localY * Math.cos(angle);\n        return projectFacePoint(pose, surface, centerX + rotatedX, centerY + rotatedY);\n    });\n};\nconst visiblePath = (points) => {\n    const segments = [];\n    let segment = [];\n    points.forEach(({ point, normal }) => {\n        if (normal[2] > 0)\n            segment.push(point);\n        else if (segment.length) {\n            segments.push(segment);\n            segment = [];\n        }\n    });\n    if (segment.length)\n        segments.push(segment);\n    return segments\n        .filter(item => item.length > 1)\n        .map(item => path(item, false))\n        .join('');\n};\nconst wirePaths = (pose, surface) => {\n    const key = surfaceCacheKey(surface);\n    let samples = wireSamplesCache.get(key);\n    if (!samples) {\n        const parallels = [-60, -30, 0, 30, 60].map(latitude => Array.from({ length: 73 }, (_, index) => localSurfacePoint(surface, (0, exports.radians)(-180 + index * 5), (0, exports.radians)(latitude))));\n        const meridians = Array.from({ length: 12 }, (_, longitudeIndex) => -150 + longitudeIndex * 30).map(longitude => Array.from({ length: 37 }, (_, index) => localSurfacePoint(surface, (0, exports.radians)(longitude), (0, exports.radians)(-90 + index * 5))));\n        samples = cacheSurfaceValue(wireSamplesCache, key, [...parallels, ...meridians]);\n    }\n    return samples.map(curve => visiblePath(curve.map(sample => projectLocalSurfacePoint(pose, sample))));\n};\nconst projectEyePoint = (pose, surface, side, localX, localY) => {\n    const expression = pose.expression;\n    const suffix = side < 0 ? 'Left' : 'Right';\n    const angle = (0, exports.radians)(side < 0 ? expression.leftAngle : expression.rightAngle);\n    const rotatedX = localX * Math.cos(angle) - localY * Math.sin(angle);\n    const rotatedY = localX * Math.sin(angle) + localY * Math.cos(angle);\n    return projectFacePoint(pose, surface, (side * expression.spacing) / 2 + expression[`positionX${suffix}`] + rotatedX, expression[`positionY${suffix}`] + rotatedY).point;\n};\nconst renderEyeEditor = (pose, surface, side) => {\n    const expression = pose.expression;\n    const suffix = side < 0 ? 'Left' : 'Right';\n    const width = expression[`width${suffix}`];\n    const height = expression[`height${suffix}`];\n    const selectedSamples = eyePoints(pose, surface, side, 1);\n    const selectedPoints = selectedSamples.map(sample => sample.point);\n    const center = projectEyePoint(pose, surface, side, 0, 0);\n    const widthHandle = projectEyePoint(pose, surface, side, width / 2 + 9, 0);\n    const heightHandle = projectEyePoint(pose, surface, side, 0, -height / 2 - 9);\n    const rotateHandle = projectEyePoint(pose, surface, side, 0, -height / 2 - 30);\n    const sizeHandle = projectEyePoint(pose, surface, side, width / 2 + 11, height / 2 + 11);\n    const leftCenter = projectEyePoint(pose, surface, -1, 0, 0);\n    const rightCenter = projectEyePoint(pose, surface, 1, 0, 0);\n    const spacingCenterX = (expression.positionXLeft + expression.positionXRight) / 2;\n    const spacingCenterY = (expression.positionYLeft + expression.positionYRight) / 2;\n    const spacingHandle = projectFacePoint(pose, surface, spacingCenterX, spacingCenterY + height / 2 + 34).point;\n    const spacingMiddle = [\n        (leftCenter[0] + rightCenter[0]) / 2,\n        (leftCenter[1] + rightCenter[1]) / 2,\n        (leftCenter[2] + rightCenter[2]) / 2,\n    ];\n    const line = (from, to) => path([from, to], false);\n    return {\n        visible: selectedSamples.reduce((total, sample) => total + sample.normal[2], 0) > 0,\n        selectionPath: path(selectedPoints),\n        widthGuide: line(center, widthHandle),\n        heightGuide: line(center, heightHandle),\n        rotationGuide: line(heightHandle, rotateHandle),\n        spacingGuide: `${line(leftCenter, rightCenter)}${line(spacingMiddle, spacingHandle)}`,\n        center,\n        widthHandle,\n        heightHandle,\n        rotateHandle,\n        sizeHandle,\n        spacingHandle,\n    };\n};\nexports.renderEyeEditor = renderEyeEditor;\nconst convexHull = (points) => {\n    const sorted = [...points].sort((left, right) => left[0] - right[0] || left[1] - right[1]);\n    const cross = (origin, first, second) => (first[0] - origin[0]) * (second[1] - origin[1]) -\n        (first[1] - origin[1]) * (second[0] - origin[0]);\n    const half = (source) => {\n        const result = [];\n        source.forEach(point => {\n            while (result.length >= 2 && cross(result.at(-2), result.at(-1), point) <= 0)\n                result.pop();\n            result.push(point);\n        });\n        return result;\n    };\n    return [...half(sorted).slice(0, -1), ...half(sorted.reverse()).slice(0, -1)];\n};\nconst smoothClosedPath = (points) => {\n    if (points.length < 3)\n        return path(points);\n    const pointAt = (index) => points[(index + points.length) % points.length];\n    return `M${points[0][0].toFixed(2)} ${points[0][1].toFixed(2)}${points\n        .map((point, index) => {\n        const previous = pointAt(index - 1);\n        const next = pointAt(index + 1);\n        const afterNext = pointAt(index + 2);\n        const firstControl = [\n            point[0] + (next[0] - previous[0]) / 6,\n            point[1] + (next[1] - previous[1]) / 6,\n            point[2],\n        ];\n        const secondControl = [\n            next[0] - (afterNext[0] - point[0]) / 6,\n            next[1] - (afterNext[1] - point[1]) / 6,\n            next[2],\n        ];\n        return `C${firstControl[0].toFixed(2)} ${firstControl[1].toFixed(2)} ${secondControl[0].toFixed(2)} ${secondControl[1].toFixed(2)} ${next[0].toFixed(2)} ${next[1].toFixed(2)}`;\n    })\n        .join('')}Z`;\n};\nconst densifyClosedPoints = (points, maximumDistance = 7) => points.flatMap((point, index) => {\n    const next = points[(index + 1) % points.length];\n    const steps = Math.max(1, Math.ceil(Math.hypot(next[0] - point[0], next[1] - point[1]) / maximumDistance));\n    return Array.from({ length: steps }, (_, step) => {\n        const progress = step / steps;\n        return [\n            point[0] + (next[0] - point[0]) * progress,\n            point[1] + (next[1] - point[1]) * progress,\n            point[2] + (next[2] - point[2]) * progress,\n        ];\n    });\n});\nconst smoothOpenPath = (points) => {\n    if (!points.length)\n        return '';\n    if (points.length === 1)\n        return `${points[0][0].toFixed(2)} ${points[0][1].toFixed(2)}`;\n    return points\n        .slice(0, -1)\n        .map((point, index) => {\n        const previous = points[Math.max(0, index - 1)];\n        const next = points[index + 1];\n        const afterNext = points[Math.min(points.length - 1, index + 2)];\n        const firstControlX = point[0] + (next[0] - previous[0]) / 6;\n        const firstControlY = point[1] + (next[1] - previous[1]) / 6;\n        const secondControlX = next[0] - (afterNext[0] - point[0]) / 6;\n        const secondControlY = next[1] - (afterNext[1] - point[1]) / 6;\n        return `C${firstControlX.toFixed(2)} ${firstControlY.toFixed(2)} ${secondControlX.toFixed(2)} ${secondControlY.toFixed(2)} ${next[0].toFixed(2)} ${next[1].toFixed(2)}`;\n    })\n        .join('');\n};\nconst projectLocalPoint = (pose, point) => project((0, exports.rotateWithQuaternion)(pose.orientation, point), pose.expression.perspective);\nconst ringPoints = (width, depth, y) => Array.from({ length: PRIMITIVE_RING_SAMPLES + 1 }, (_, index) => {\n    const angle = (index / PRIMITIVE_RING_SAMPLES) * Math.PI * 2;\n    return [(width / 2) * Math.sin(angle), y, (depth / 2) * Math.cos(angle)];\n});\nconst projectedRoundedPrimitivePath = (pose, surface) => {\n    const key = surfaceCacheKey(surface);\n    let localSamples = headSamplesCache.get(key);\n    if (!localSamples) {\n        localSamples = Array.from({ length: ROUNDED_PRIMITIVE_LATITUDE_SAMPLES }, (_, latitudeIndex) => {\n            const latitude = -Math.PI / 2 + (latitudeIndex / (ROUNDED_PRIMITIVE_LATITUDE_SAMPLES - 1)) * Math.PI;\n            return Array.from({ length: ROUNDED_PRIMITIVE_LONGITUDE_SAMPLES }, (_, longitudeIndex) => {\n                const longitude = -Math.PI + (longitudeIndex / (ROUNDED_PRIMITIVE_LONGITUDE_SAMPLES - 1)) * Math.PI * 2;\n                return (0, surfaces_1.surfacePointAt)(surface, longitude, latitude);\n            });\n        }).flat();\n        cacheSurfaceValue(headSamplesCache, key, localSamples);\n    }\n    const projected = localSamples.map(point => projectLocalPoint(pose, point));\n    return smoothClosedPath(densifyClosedPoints(convexHull(projected)));\n};\nconst projectedCylinderPath = (pose, surface) => {\n    if (surface.roundness > 0 || (surface.morphRoundness ?? 0) > 0) {\n        return projectedRoundedPrimitivePath(pose, surface);\n    }\n    const halfHeight = surface.height / 2;\n    const projected = [\n        ...ringPoints(surface.width, surface.depth, -halfHeight),\n        ...ringPoints(surface.width, surface.depth, halfHeight),\n    ].map(point => projectLocalPoint(pose, point));\n    return smoothClosedPath(densifyClosedPoints(convexHull(projected)));\n};\nconst projectedCursorBodyPath = (pose, surface) => {\n    const layout = (0, surfaces_1.cursorLayout)(surface);\n    const halfHeight = layout.bodyHeight / 2;\n    const projected = [\n        ...ringPoints(layout.bodyWidth, layout.bodyDepth, layout.bodyCenterY - halfHeight),\n        ...ringPoints(layout.bodyWidth, layout.bodyDepth, layout.bodyCenterY + halfHeight),\n    ].map(point => projectLocalPoint(pose, point));\n    return smoothClosedPath(densifyClosedPoints(convexHull(projected)));\n};\nconst projectedCursorConePath = (pose, surface) => {\n    const layout = (0, surfaces_1.cursorLayout)(surface);\n    const apex = projectLocalPoint(pose, [0, layout.coneApexY, 0]);\n    const base = ringPoints(surface.width, surface.depth, layout.coneBaseY).map(point => projectLocalPoint(pose, point));\n    return smoothClosedPath(densifyClosedPoints(convexHull([...base, apex])));\n};\nconst projectedConePath = (pose, surface) => {\n    if ((surface.morphRoundness ?? 0) > 0 ||\n        (surface.tipRoundness ?? 0) > 0 ||\n        (surface.baseRoundness ?? 0) > 0) {\n        return projectedRoundedPrimitivePath(pose, surface);\n    }\n    const apex = projectLocalPoint(pose, [0, -surface.height / 2, 0]);\n    const base = ringPoints(surface.width, surface.depth, surface.height / 2).map(point => projectLocalPoint(pose, point));\n    const hull = convexHull([...base, apex]);\n    const apexIndex = hull.findIndex(point => Math.hypot(point[0] - apex[0], point[1] - apex[1]) < 0.01);\n    if (apexIndex < 0)\n        return smoothClosedPath(hull);\n    const ordered = [...hull.slice(apexIndex), ...hull.slice(0, apexIndex)];\n    const baseArc = ordered.slice(1);\n    if (baseArc.length < 2)\n        return path(hull);\n    return `M${apex[0].toFixed(2)} ${apex[1].toFixed(2)}L${baseArc[0][0].toFixed(2)} ${baseArc[0][1].toFixed(2)}${smoothOpenPath(baseArc)}L${apex[0].toFixed(2)} ${apex[1].toFixed(2)}Z`;\n};\nconst projectedCubePath = (pose, surface) => {\n    if (surface.roundness > 0)\n        return projectedRoundedPrimitivePath(pose, surface);\n    const halfWidth = surface.width / 2;\n    const halfHeight = surface.height / 2;\n    const halfDepth = surface.depth / 2;\n    const vertices = [-1, 1].flatMap(x => [-1, 1].flatMap(y => [-1, 1].map(z => [x * halfWidth, y * halfHeight, z * halfDepth])));\n    return path(convexHull(vertices.map(point => projectLocalPoint(pose, point))));\n};\nconst projectedDiamondPath = (pose, surface) => {\n    if (surface.roundness > 0)\n        return projectedRoundedPrimitivePath(pose, surface);\n    const halfWidth = surface.width / 2;\n    const halfHeight = surface.height / 2;\n    const halfDepth = surface.depth / 2;\n    const vertices = [\n        [-halfWidth, 0, 0],\n        [halfWidth, 0, 0],\n        [0, -halfHeight, 0],\n        [0, halfHeight, 0],\n        [0, 0, -halfDepth],\n        [0, 0, halfDepth],\n    ];\n    return path(convexHull(vertices.map(point => projectLocalPoint(pose, point))));\n};\nconst ellipseProjection = (centerX, centerY, covarianceXX, covarianceXY, covarianceYY) => {\n    const trace = covarianceXX + covarianceYY;\n    const difference = Math.hypot(covarianceXX - covarianceYY, covarianceXY * 2);\n    const majorSquared = (trace + difference) / 2;\n    const minorSquared = (trace - difference) / 2;\n    if (majorSquared <= 0 || minorSquared <= 0)\n        return null;\n    return {\n        centerX,\n        centerY,\n        majorRadius: Math.sqrt(majorSquared),\n        minorRadius: Math.sqrt(minorSquared),\n        rotation: Math.atan2(covarianceXY * 2, covarianceXX - covarianceYY) / 2,\n    };\n};\nconst ellipsePath = ({ centerX, centerY, majorRadius, minorRadius, rotation, }) => {\n    const rotationDegrees = (rotation * 180) / Math.PI;\n    const offsetX = Math.cos(rotation) * majorRadius;\n    const offsetY = Math.sin(rotation) * majorRadius;\n    const startX = centerX + offsetX;\n    const startY = centerY + offsetY;\n    const endX = centerX - offsetX;\n    const endY = centerY - offsetY;\n    return `M${startX.toFixed(2)} ${startY.toFixed(2)}A${majorRadius.toFixed(2)} ${minorRadius.toFixed(2)} ${rotationDegrees.toFixed(2)} 0 1 ${endX.toFixed(2)} ${endY.toFixed(2)}A${majorRadius.toFixed(2)} ${minorRadius.toFixed(2)} ${rotationDegrees.toFixed(2)} 0 1 ${startX.toFixed(2)} ${startY.toFixed(2)}Z`;\n};\nconst projectedEllipsoid = (pose, axes, localCenter = [0, 0, 0]) => {\n    const rotatedAxes = [\n        (0, exports.rotateWithQuaternion)(pose.orientation, [1, 0, 0]),\n        (0, exports.rotateWithQuaternion)(pose.orientation, [0, 1, 0]),\n        (0, exports.rotateWithQuaternion)(pose.orientation, [0, 0, 1]),\n    ];\n    const center = (0, exports.rotateWithQuaternion)(pose.orientation, localCenter);\n    if (Math.abs(pose.expression.perspective) < 0.0001) {\n        const covarianceXX = rotatedAxes.reduce((total, axis, index) => total + axis[0] * axis[0] * axes[index] * axes[index], 0);\n        const covarianceXY = rotatedAxes.reduce((total, axis, index) => total + axis[0] * axis[1] * axes[index] * axes[index], 0);\n        const covarianceYY = rotatedAxes.reduce((total, axis, index) => total + axis[1] * axis[1] * axes[index] * axes[index], 0);\n        return ellipseProjection(center[0], center[1], covarianceXX, covarianceXY, covarianceYY);\n    }\n    const inverseAxesSquared = axes.map(axis => 1 / (axis * axis));\n    const quadratic = Array.from({ length: 3 }, (_, row) => Array.from({ length: 3 }, (_, column) => rotatedAxes.reduce((total, axis, index) => total + axis[row] * inverseAxesSquared[index] * axis[column], 0)));\n    const focalLength = FOCAL_LENGTH / pose.expression.perspective;\n    const cameraOffset = [-center[0], -center[1], focalLength - center[2]];\n    const cameraNormal = [\n        quadratic[0][0] * cameraOffset[0] +\n            quadratic[0][1] * cameraOffset[1] +\n            quadratic[0][2] * cameraOffset[2],\n        quadratic[1][0] * cameraOffset[0] +\n            quadratic[1][1] * cameraOffset[1] +\n            quadratic[1][2] * cameraOffset[2],\n        quadratic[2][0] * cameraOffset[0] +\n            quadratic[2][1] * cameraOffset[1] +\n            quadratic[2][2] * cameraOffset[2],\n    ];\n    const cameraTerm = cameraOffset[0] * cameraNormal[0] +\n        cameraOffset[1] * cameraNormal[1] +\n        cameraOffset[2] * cameraNormal[2] -\n        1;\n    const tangentLinear = [cameraNormal[0], cameraNormal[1], -focalLength * cameraNormal[2]];\n    const rayQuadratic = [\n        [quadratic[0][0], quadratic[0][1], -focalLength * quadratic[0][2]],\n        [quadratic[1][0], quadratic[1][1], -focalLength * quadratic[1][2]],\n        [\n            -focalLength * quadratic[2][0],\n            -focalLength * quadratic[2][1],\n            focalLength * focalLength * quadratic[2][2],\n        ],\n    ];\n    const conic = Array.from({ length: 3 }, (_, row) => Array.from({ length: 3 }, (_, column) => tangentLinear[row] * tangentLinear[column] - cameraTerm * rayQuadratic[row][column]));\n    const determinant = conic[0][0] * conic[1][1] - conic[0][1] * conic[0][1];\n    if (Math.abs(determinant) < 1e-12)\n        return null;\n    const centerX = -(conic[1][1] * conic[0][2] - conic[0][1] * conic[1][2]) / determinant;\n    const centerY = (conic[0][1] * conic[0][2] - conic[0][0] * conic[1][2]) / determinant;\n    const centeredConstant = conic[2][2] + conic[0][2] * centerX + conic[1][2] * centerY;\n    const scale = -centeredConstant;\n    if (Math.abs(scale) < 1e-12)\n        return null;\n    const shapeXX = conic[0][0] / scale;\n    const shapeXY = conic[0][1] / scale;\n    const shapeYY = conic[1][1] / scale;\n    const shapeDeterminant = shapeXX * shapeYY - shapeXY * shapeXY;\n    if (shapeDeterminant <= 0)\n        return null;\n    return ellipseProjection(centerX, centerY, shapeYY / shapeDeterminant, -shapeXY / shapeDeterminant, shapeXX / shapeDeterminant);\n};\nconst projectedEllipsoidPath = (pose, surface) => {\n    const ellipse = projectedEllipsoid(pose, [\n        surface.width / 2,\n        surface.height / 2,\n        surface.depth / 2,\n    ]);\n    const isSphere = surface.width === surface.height && surface.height === surface.depth;\n    if (ellipse && isSphere) {\n        const radius = (ellipse.majorRadius + ellipse.minorRadius) / 2;\n        return ellipsePath({\n            centerX: 0,\n            centerY: 0,\n            majorRadius: radius,\n            minorRadius: radius,\n            rotation: 0,\n        });\n    }\n    return ellipse ? ellipsePath(ellipse) : null;\n};\nconst mickeyEarPaths = (pose, surface) => {\n    if (surface.type !== 'mickey')\n        return [];\n    const radius = Math.min(surface.width, surface.height) * 0.23;\n    const depthRadius = Math.min(radius, surface.depth * 0.29);\n    const centerX = surface.width * 0.37;\n    const centerY = -surface.height * 0.39;\n    const centerZ = -surface.depth * 0.12;\n    const axes = [radius, radius, depthRadius];\n    return [-1, 1]\n        .map(side => projectedEllipsoid(pose, axes, [side * centerX, centerY, centerZ]))\n        .filter((ear) => ear !== null)\n        .map(ellipsePath);\n};\nconst compositeBackPaths = (pose, surface) => {\n    if (surface.type === 'mickey')\n        return mickeyEarPaths(pose, surface);\n    if (surface.type === 'cursor')\n        return [projectedCursorConePath(pose, surface)];\n    return [];\n};\nconst ellipsePoints = (ellipse) => Array.from({ length: PRIMITIVE_RING_SAMPLES }, (_, index) => {\n    const angle = (index / PRIMITIVE_RING_SAMPLES) * Math.PI * 2;\n    const major = Math.cos(angle) * ellipse.majorRadius;\n    const minor = Math.sin(angle) * ellipse.minorRadius;\n    return [\n        ellipse.centerX + major * Math.cos(ellipse.rotation) - minor * Math.sin(ellipse.rotation),\n        ellipse.centerY + major * Math.sin(ellipse.rotation) + minor * Math.cos(ellipse.rotation),\n        0,\n    ];\n});\nconst smoothHullPath = (points) => {\n    if (points.length < 3)\n        return path(points);\n    const distances = points.map((point, index) => {\n        const next = points[(index + 1) % points.length];\n        return Math.hypot(next[0] - point[0], next[1] - point[1]);\n    });\n    const sortedDistances = [...distances].sort((left, right) => left - right);\n    const medianDistance = sortedDistances[Math.floor(sortedDistances.length / 2)] || 1;\n    const straightThreshold = Math.max(8, medianDistance * 3.5);\n    const straightEdges = distances.map(distance => distance > straightThreshold);\n    return `M${points[0][0].toFixed(2)} ${points[0][1].toFixed(2)}${points\n        .map((point, index) => {\n        const nextIndex = (index + 1) % points.length;\n        const next = points[nextIndex];\n        if (straightEdges[index])\n            return `L${next[0].toFixed(2)} ${next[1].toFixed(2)}`;\n        const previous = straightEdges[(index - 1 + points.length) % points.length]\n            ? point\n            : points[(index - 1 + points.length) % points.length];\n        const afterNext = straightEdges[nextIndex] ? next : points[(index + 2) % points.length];\n        const firstControlX = point[0] + (next[0] - previous[0]) / 6;\n        const firstControlY = point[1] + (next[1] - previous[1]) / 6;\n        const secondControlX = next[0] - (afterNext[0] - point[0]) / 6;\n        const secondControlY = next[1] - (afterNext[1] - point[1]) / 6;\n        return `C${firstControlX.toFixed(2)} ${firstControlY.toFixed(2)} ${secondControlX.toFixed(2)} ${secondControlY.toFixed(2)} ${next[0].toFixed(2)} ${next[1].toFixed(2)}`;\n    })\n        .join('')}Z`;\n};\nconst projectedCapsulePath = (pose, surface) => {\n    const radiusX = surface.width / 2;\n    const radiusY = Math.min(radiusX, surface.height / 2);\n    const radiusZ = surface.depth / 2;\n    const straightHalf = Math.max(0, (surface.height - radiusY * 2) / 2);\n    const axes = [radiusX, radiusY, radiusZ];\n    const top = projectedEllipsoid(pose, axes, [0, straightHalf, 0]);\n    const bottom = projectedEllipsoid(pose, axes, [0, -straightHalf, 0]);\n    if (!top || !bottom)\n        return null;\n    return smoothHullPath(convexHull([...ellipsePoints(top), ...ellipsePoints(bottom)]));\n};\nconst sharpProfileTypes = new Set([\n    'star',\n    'book',\n    'hand',\n    'droplet',\n    'pyramid',\n    'hexagon',\n    'shield',\n    'gem',\n]);\nconst projectedSurfaceProfilePath = (pose, surface) => {\n    const profile = (0, surfaces_1.surfaceProfilePoints)(surface);\n    if (!profile?.length)\n        return '';\n    const projected = profile.map(([x, y]) => projectLocalPoint(pose, [x, y, 0]));\n    return sharpProfileTypes.has(surface.type)\n        ? path(projected)\n        : smoothClosedPath(densifyClosedPoints(projected));\n};\nconst headPath = (pose, surface) => {\n    const profilePath = projectedSurfaceProfilePath(pose, surface);\n    if (profilePath)\n        return profilePath;\n    if (surface.type === 'sphere' || surface.type === 'mickey') {\n        const exactPath = projectedEllipsoidPath(pose, surface);\n        if (exactPath)\n            return exactPath;\n    }\n    if (surface.type === 'capsule') {\n        const exactPath = projectedCapsulePath(pose, surface);\n        if (exactPath)\n            return exactPath;\n    }\n    if (surface.type === 'cylinder')\n        return projectedCylinderPath(pose, surface);\n    if (surface.type === 'cursor')\n        return projectedCursorBodyPath(pose, surface);\n    if (surface.type === 'cone')\n        return projectedConePath(pose, surface);\n    if (surface.type === 'cube')\n        return projectedCubePath(pose, surface);\n    if (surface.type === 'diamond')\n        return projectedDiamondPath(pose, surface);\n    const key = surfaceCacheKey(surface);\n    let localSamples = headSamplesCache.get(key);\n    if (!localSamples) {\n        localSamples = Array.from({ length: HEAD_LATITUDE_SAMPLES }, (_, latitudeIndex) => {\n            const latitude = -Math.PI / 2 + (latitudeIndex / (HEAD_LATITUDE_SAMPLES - 1)) * Math.PI;\n            return Array.from({ length: HEAD_LONGITUDE_SAMPLES }, (_, longitudeIndex) => {\n                const longitude = -Math.PI + (longitudeIndex / (HEAD_LONGITUDE_SAMPLES - 1)) * Math.PI * 2;\n                return (0, surfaces_1.surfacePointAt)(surface, longitude, latitude);\n            });\n        }).flat();\n        cacheSurfaceValue(headSamplesCache, key, localSamples);\n    }\n    const projectedSamples = localSamples.map(sample => project((0, exports.rotateWithQuaternion)(pose.orientation, sample), pose.expression.perspective));\n    return path(convexHull(projectedSamples));\n};\nconst accessoryPath = (pose, node) => {\n    const profile = (0, surfaces_1.surfaceProfilePoints)(node.surface);\n    if (profile?.length) {\n        const localOrientation = (0, exports.quaternionFromEuler)((0, exports.radians)(node.rotation[0]), (0, exports.radians)(node.rotation[1]), (0, exports.radians)(node.rotation[2]));\n        const projected = profile.map(([x, y]) => {\n            const locallyRotated = (0, exports.rotateWithQuaternion)(localOrientation, [x, y, 0]);\n            const positioned = [\n                locallyRotated[0] + node.position[0],\n                locallyRotated[1] + node.position[1],\n                locallyRotated[2] + node.position[2],\n            ];\n            return project((0, exports.rotateWithQuaternion)(pose.orientation, positioned), pose.expression.perspective);\n        });\n        return sharpProfileTypes.has(node.surface.type)\n            ? path(projected)\n            : smoothClosedPath(densifyClosedPoints(projected));\n    }\n    const key = surfaceCacheKey(node.surface);\n    let localSamples = accessorySamplesCache.get(key);\n    if (!localSamples) {\n        localSamples = Array.from({ length: 17 }, (_, latitudeIndex) => {\n            const latitude = -Math.PI / 2 + (latitudeIndex / 16) * Math.PI;\n            return Array.from({ length: 49 }, (_, longitudeIndex) => {\n                const longitude = -Math.PI + (longitudeIndex / 48) * Math.PI * 2;\n                return (0, surfaces_1.surfacePointAt)(node.surface, longitude, latitude);\n            });\n        }).flat();\n        cacheSurfaceValue(accessorySamplesCache, key, localSamples);\n    }\n    const localOrientation = (0, exports.quaternionFromEuler)((0, exports.radians)(node.rotation[0]), (0, exports.radians)(node.rotation[1]), (0, exports.radians)(node.rotation[2]));\n    const projected = localSamples.map(point => {\n        const locallyRotated = (0, exports.rotateWithQuaternion)(localOrientation, point);\n        const positioned = [\n            locallyRotated[0] + node.position[0],\n            locallyRotated[1] + node.position[1],\n            locallyRotated[2] + node.position[2],\n        ];\n        return project((0, exports.rotateWithQuaternion)(pose.orientation, positioned), pose.expression.perspective);\n    });\n    const hull = convexHull(projected);\n    if ((node.surface.type === 'cube' || node.surface.type === 'diamond') &&\n        node.surface.roundness <= 0) {\n        return path(hull);\n    }\n    return smoothClosedPath(densifyClosedPoints(hull));\n};\nconst ACCESSORY_FRONT_CROSSING_RATIO = 0.1;\nconst accessoryCameraDepthRadius = (pose, node) => {\n    const localOrientation = (0, exports.quaternionFromEuler)((0, exports.radians)(node.rotation[0]), (0, exports.radians)(node.rotation[1]), (0, exports.radians)(node.rotation[2]));\n    const cameraDepthByAxis = [\n        [1, 0, 0],\n        [0, 1, 0],\n        [0, 0, 1],\n    ].map(axis => (0, exports.rotateWithQuaternion)(pose.orientation, (0, exports.rotateWithQuaternion)(localOrientation, axis))[2]);\n    return Math.hypot(cameraDepthByAxis[0] * (node.surface.width / 2), cameraDepthByAxis[1] * (node.surface.height / 2), cameraDepthByAxis[2] * (node.surface.depth / 2));\n};\nconst accessoryLayers = (pose, nodes) => {\n    const layers = nodes\n        .map(node => {\n        const depth = (0, exports.rotateWithQuaternion)(pose.orientation, node.position)[2];\n        return {\n            id: node.id,\n            path: accessoryPath(pose, node),\n            depth,\n            front: depth > accessoryCameraDepthRadius(pose, node) * ACCESSORY_FRONT_CROSSING_RATIO,\n        };\n    })\n        .sort((left, right) => left.depth - right.depth);\n    return {\n        backPaths: layers.filter(layer => !layer.front).map(layer => layer.path),\n        frontPaths: layers.filter(layer => layer.front).map(layer => layer.path),\n        backNodeIds: layers.filter(layer => !layer.front).map(layer => layer.id),\n        frontNodeIds: layers.filter(layer => layer.front).map(layer => layer.id),\n    };\n};\nconst mouthPoints = (pose, surface, type = 'none', scale = 1) => {\n    if (!type || type === 'none')\n        return { path: '', visible: false };\n    const mouthY = 22;\n    const mouthHalfWidth = 20 * (scale || 1);\n    if (type === 'oMouth' || type === 'kiss') {\n        const points = [];\n        let totalNormalZ = 0;\n        const horizontalRadius = (type === 'kiss' ? 8 : 13) * (scale || 1);\n        const verticalRadius = (type === 'kiss' ? 6.5 : 12) * (scale || 1);\n        for (let index = 0; index <= 20; index += 1) {\n            const angle = (index / 20) * Math.PI * 2;\n            const x = Math.cos(angle) * horizontalRadius;\n            const y = mouthY + Math.sin(angle) * verticalRadius;\n            const projected = projectFacePoint(pose, surface, x, y);\n            points.push(projected.point);\n            totalNormalZ += projected.normal[2];\n        }\n        if (!points.length)\n            return { path: '', visible: false };\n        const d = `${path(points)}Z`;\n        return { path: d, visible: totalNormalZ > -0.1 };\n    }\n    const sampleCount = 15;\n    const points = [];\n    let totalNormalZ = 0;\n    for (let index = 0; index < sampleCount; index += 1) {\n        const t = (index / (sampleCount - 1)) * 2 - 1;\n        const x = t * mouthHalfWidth * (type === 'grin' ? 1.2 : 1);\n        const baseCurve = type === 'smile'\n            ? -7\n            : type === 'openSmile' || type === 'grin'\n                ? -12\n                : type === 'frown'\n                    ? 7\n                    : 0;\n        const catCurve = type === 'cat' ? -5 * Math.abs(Math.sin(t * Math.PI)) + 2.5 : 0;\n        const smirkCurve = type === 'smirk' ? (-6 * (t + 1)) / 2 : 0;\n        const curveOffset = (1 - t * t) * baseCurve + catCurve + smirkCurve;\n        const y = mouthY + curveOffset;\n        const projected = projectFacePoint(pose, surface, x, y);\n        points.push(projected.point);\n        totalNormalZ += projected.normal[2];\n    }\n    if (points.length < 2)\n        return { path: '', visible: false };\n    return { path: path(points, false), visible: totalNormalZ > -0.1 };\n};\nconst projectedSurfaceBand = (pose, surface, latFn1, latFn2) => {\n    const steps = 36;\n    const topPoints = [];\n    const bottomPoints = [];\n    for (let i = 0; i <= steps; i++) {\n        const long = -Math.PI + (i / steps) * Math.PI * 2;\n        const lat1 = latFn1(long);\n        const lat2 = latFn2(long);\n        const p1 = (0, surfaces_1.surfacePointAt)(surface, long, lat1);\n        const p2 = (0, surfaces_1.surfacePointAt)(surface, long, lat2);\n        topPoints.push(project((0, exports.rotateWithQuaternion)(pose.orientation, p1), pose.expression.perspective));\n        bottomPoints.push(project((0, exports.rotateWithQuaternion)(pose.orientation, p2), pose.expression.perspective));\n    }\n    const allPoints = [...topPoints, ...bottomPoints.reverse()];\n    if (allPoints.length < 3)\n        return '';\n    let d = `M${allPoints[0][0].toFixed(2)} ${allPoints[0][1].toFixed(2)}`;\n    for (let i = 1; i < allPoints.length; i++) {\n        d += ` L${allPoints[i][0].toFixed(2)} ${allPoints[i][1].toFixed(2)}`;\n    }\n    return `${d}Z`;\n};\nconst projectedSurfaceBottomFill = (pose, surface, latFn) => {\n    const steps = 36;\n    const points = [];\n    for (let i = 0; i <= steps; i++) {\n        const long = -Math.PI + (i / steps) * Math.PI * 2;\n        const lat = latFn(long);\n        const p = (0, surfaces_1.surfacePointAt)(surface, long, lat);\n        points.push(project((0, exports.rotateWithQuaternion)(pose.orientation, p), pose.expression.perspective));\n    }\n    points.push([450, 450, 0], [-450, 450, 0]);\n    if (points.length < 3)\n        return '';\n    let d = `M${points[0][0].toFixed(2)} ${points[0][1].toFixed(2)}`;\n    for (let i = 1; i < points.length; i++) {\n        d += ` L${points[i][0].toFixed(2)} ${points[i][1].toFixed(2)}`;\n    }\n    return `${d}Z`;\n};\nconst projectedBookDecals = (pose, surface) => {\n    if (surface.pattern !== 'book')\n        return [];\n    const LONGITUDE_STEPS = 144;\n    const BASE_FILL_ROWS = 14;\n    const BAND_ROWS = 2;\n    const FRONT_EPSILON = 1e-5;\n    const BOTTOM_LATITUDE = Math.PI / 2 - 0.001;\n    const BAND_EDGE_LATITUDE = 0.4;\n    const edgeLatitude = (_longitude) => BAND_EDGE_LATITUDE;\n    const visibility = ({ u, v }) => {\n        const point = (0, exports.rotateWithQuaternion)(pose.orientation, (0, surfaces_1.surfacePointAt)(surface, u, v));\n        const normal = (0, exports.rotateWithQuaternion)(pose.orientation, (0, surfaces_1.surfaceNormalAt)(surface, u, v));\n        const perspective = pose.expression.perspective;\n        if (Math.abs(perspective) < 1e-6)\n            return normal[2] - FRONT_EPSILON;\n        const cameraZ = FOCAL_LENGTH / perspective;\n        const viewX = -point[0];\n        const viewY = -point[1];\n        const viewZ = cameraZ - point[2];\n        const viewLength = Math.hypot(viewX, viewY, viewZ) || 1;\n        return (normal[0] * viewX + normal[1] * viewY + normal[2] * viewZ) / viewLength - FRONT_EPSILON;\n    };\n    const frontIntersection = (from, to) => {\n        let inside = visibility(from) >= 0 ? from : to;\n        let outside = visibility(from) >= 0 ? to : from;\n        for (let iteration = 0; iteration < 12; iteration += 1) {\n            const midpoint = {\n                u: (inside.u + outside.u) / 2,\n                v: (inside.v + outside.v) / 2,\n            };\n            if (visibility(midpoint) >= 0)\n                inside = midpoint;\n            else\n                outside = midpoint;\n        }\n        return inside;\n    };\n    const clipToFront = (polygon) => {\n        if (polygon.length < 3)\n            return [];\n        const clipped = [];\n        for (let index = 0; index < polygon.length; index += 1) {\n            const current = polygon[index];\n            const next = polygon[(index + 1) % polygon.length];\n            const currentInside = visibility(current) >= 0;\n            const nextInside = visibility(next) >= 0;\n            if (currentInside && nextInside) {\n                clipped.push(next);\n            }\n            else if (currentInside && !nextInside) {\n                clipped.push(frontIntersection(current, next));\n            }\n            else if (!currentInside && nextInside) {\n                clipped.push(frontIntersection(current, next), next);\n            }\n        }\n        return clipped;\n    };\n    const projectedSubpath = (polygon) => {\n        const visible = clipToFront(polygon);\n        if (visible.length < 3)\n            return '';\n        const projected = visible.map(({ u, v }) => project((0, exports.rotateWithQuaternion)(pose.orientation, (0, surfaces_1.surfacePointAt)(surface, u, v)), pose.expression.perspective));\n        let pathData = `M${projected[0][0].toFixed(2)} ${projected[0][1].toFixed(2)}`;\n        for (let index = 1; index < projected.length; index += 1) {\n            pathData += ` L${projected[index][0].toFixed(2)} ${projected[index][1].toFixed(2)}`;\n        }\n        return `${pathData}Z`;\n    };\n    const surfaceStripPath = (topLatitude, bottomLatitude, rows) => {\n        let pathData = '';\n        for (let longitudeIndex = 0; longitudeIndex < LONGITUDE_STEPS; longitudeIndex += 1) {\n            const u0 = -Math.PI + (longitudeIndex / LONGITUDE_STEPS) * Math.PI * 2;\n            const u1 = -Math.PI + ((longitudeIndex + 1) / LONGITUDE_STEPS) * Math.PI * 2;\n            const top0 = topLatitude(u0);\n            const top1 = topLatitude(u1);\n            const bottom0 = bottomLatitude(u0);\n            const bottom1 = bottomLatitude(u1);\n            for (let row = 0; row < rows; row += 1) {\n                const t0 = row / rows;\n                const t1 = (row + 1) / rows;\n                const v00 = top0 + (bottom0 - top0) * t0;\n                const v01 = top1 + (bottom1 - top1) * t0;\n                const v11 = top1 + (bottom1 - top1) * t1;\n                const v10 = top0 + (bottom0 - top0) * t1;\n                pathData += projectedSubpath([\n                    { u: u0, v: v00 },\n                    { u: u1, v: v01 },\n                    { u: u1, v: v11 },\n                    { u: u0, v: v10 },\n                ]);\n            }\n        }\n        return pathData;\n    };\n    const coverPath = surfaceStripPath(edgeLatitude, () => BOTTOM_LATITUDE, BASE_FILL_ROWS);\n    const stripePath = surfaceStripPath(edgeLatitude, longitude => edgeLatitude(longitude) + 0.1, BAND_ROWS);\n    const trimPath = surfaceStripPath(longitude => edgeLatitude(longitude) - 0.07, edgeLatitude, BAND_ROWS);\n    return [\n        { path: coverPath, fill: '#1d4ed8', opacity: 1 },\n        { path: stripePath, fill: '#3b82f6', opacity: 1 },\n        { path: trimPath, fill: '#93c5fd', opacity: 0.92 },\n    ].filter(decal => Boolean(decal.path));\n};\nconst renderAvatar = (pose, surface, blink = 1, options = {}) => {\n    const leftSamples = eyePoints(pose, surface, -1, blink, options.eyeOffset);\n    const rightSamples = eyePoints(pose, surface, 1, blink, options.eyeOffset);\n    const leftPupilSamples = pupilPoints(pose, surface, -1, blink, options.eyeOffset);\n    const rightPupilSamples = pupilPoints(pose, surface, 1, blink, options.eyeOffset);\n    const left = leftSamples.map(sample => sample.point);\n    const right = rightSamples.map(sample => sample.point);\n    const leftPupil = leftPupilSamples.map(sample => sample.point);\n    const rightPupil = rightPupilSamples.map(sample => sample.point);\n    const bodyNodes = options.bodyNodes ?? [];\n    const accessories = accessoryLayers(pose, bodyNodes);\n    const compositePaths = compositeBackPaths(pose, surface);\n    const mouth = surface.pattern === 'book' || (pose.expression.mouth || 'none') === 'none'\n        ? { path: '', visible: false }\n        : mouthPoints(pose, surface, pose.expression.mouth || 'none', pose.expression.mouthScale || 1);\n    const decals = projectedBookDecals(pose, surface);\n    const nodeStyles = {};\n    bodyNodes.forEach(node => {\n        nodeStyles[node.id] = {\n            color: node.color,\n            colorTo: node.colorTo,\n            gradientType: node.gradientType,\n            opacity: node.opacity,\n            material: node.material,\n        };\n    });\n    return {\n        backPaths: [...compositePaths, ...accessories.backPaths],\n        frontPaths: accessories.frontPaths,\n        backNodeIds: [...compositePaths.map(() => null), ...accessories.backNodeIds],\n        frontNodeIds: accessories.frontNodeIds,\n        headPath: headPath(pose, surface),\n        leftPath: path(left),\n        rightPath: path(right),\n        leftPupilPath: path(leftPupil),\n        rightPupilPath: path(rightPupil),\n        leftVisible: leftSamples.reduce((total, sample) => total + sample.normal[2], 0) > 0,\n        rightVisible: rightSamples.reduce((total, sample) => total + sample.normal[2], 0) > 0,\n        creatureEyeFrame: creatureEyeFrame(pose, surface, blink, options.eyeOffset),\n        mouthPath: mouth.path,\n        mouthVisible: mouth.visible,\n        decals,\n        nodeStyles,\n        wirePaths: options.includeWire === false ? [] : wirePaths(pose, surface),\n    };\n};\nexports.renderAvatar = renderAvatar;\n},\"/creature/creatureExpression\":function(require,module,exports){\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.creatureEyeRigFromExpression = exports.CREATURE_FRAME_HALF_HEIGHT = exports.CREATURE_FRAME_HALF_WIDTH = exports.CREATURE_NATIVE_EYE_CENTER_Y = exports.CREATURE_NATIVE_EYE_CENTER_X = void 0;\nexports.CREATURE_NATIVE_EYE_CENTER_X = 0.379;\nexports.CREATURE_NATIVE_EYE_CENTER_Y = -0.054;\nexports.CREATURE_FRAME_HALF_WIDTH = 42;\nexports.CREATURE_FRAME_HALF_HEIGHT = 58;\nconst BASE_WIDTH = 20;\nconst BASE_HEIGHT = 50;\nconst BASE_SPACING = 35;\nconst BASE_Y = -7;\nconst clamp = (value, min, max) => Math.max(min, Math.min(max, value));\nconst widthScale = (width) => clamp(0.72 + (Math.max(10, width) / BASE_WIDTH) * 0.28, 0.72, 1.58);\nconst heightScale = (height, blink) => {\n    const authored = clamp((Math.max(5, height) - 5) / (BASE_HEIGHT - 5), 0, 1.65);\n    const expressionScale = 0.075 + Math.pow(authored, 0.78) * 0.925;\n    const blinkScale = 0.055 + clamp(blink, 0, 1) * 0.945;\n    return clamp(expressionScale * blinkScale, 0.045, 1.52);\n};\nconst gazeNeedsLock = (expression, avgX, avgY, avgHeight) => avgHeight < 30 ||\n    Math.abs(expression.heightLeft - expression.heightRight) > 7 ||\n    Math.abs(expression.leftAngle) > 4 ||\n    Math.abs(expression.rightAngle) > 4 ||\n    Math.abs(avgX) > 3 ||\n    Math.abs(avgY - BASE_Y) > 7 ||\n    expression.eyeMotion === 'wander' ||\n    expression.eyeMotion === 'lookAround' ||\n    expression.eyeMotion === 'focusPulse' ||\n    expression.eyeMotion === 'shake' ||\n    expression.eyeMotion === 'microSaccades';\nconst creatureEyeRigFromExpression = (expression, blink = 1, eyeOffset = { x: 0, y: 0 }) => {\n    const spacingShift = (expression.spacing - BASE_SPACING) / (exports.CREATURE_FRAME_HALF_WIDTH * 2);\n    const avgX = (expression.positionXLeft + expression.positionXRight) / 2 + eyeOffset.x;\n    const avgY = (expression.positionYLeft + expression.positionYRight) / 2 + eyeOffset.y;\n    const avgHeight = (expression.heightLeft + expression.heightRight) / 2;\n    const side = (suffix, direction) => ({\n        widthScale: widthScale(expression[`width${suffix}`]),\n        heightScale: heightScale(expression[`height${suffix}`], blink),\n        offsetX: direction * spacingShift +\n            (expression[`positionX${suffix}`] + eyeOffset.x) / exports.CREATURE_FRAME_HALF_WIDTH,\n        offsetY: (expression[`positionY${suffix}`] - BASE_Y + eyeOffset.y) / exports.CREATURE_FRAME_HALF_HEIGHT,\n        rotation: ((suffix === 'Left' ? expression.leftAngle : expression.rightAngle) * Math.PI) / 180,\n    });\n    const gazeX = clamp(avgX / 18, -0.82, 0.82);\n    const gazeY = clamp(-(avgY - BASE_Y) / 20, -0.62, 0.62);\n    return {\n        left: side('Left', -1),\n        right: side('Right', 1),\n        gazeX,\n        gazeY,\n        lockGaze: gazeNeedsLock(expression, avgX, avgY, avgHeight),\n    };\n};\nexports.creatureEyeRigFromExpression = creatureEyeRigFromExpression;\n},\"/avatar/ambientMotion\":function(require,module,exports){\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.applyAmbientMotion = exports.applyAmbientBodyMotion = exports.ambientEyeOffset = exports.ambientBodyOffset = exports.hasAmbientMotion = exports.isBodyMotion = exports.isEyeMotion = exports.bodyMotionModes = exports.eyeMotionModes = void 0;\nexports.eyeMotionModes = [\n    'none',\n    'microSaccades',\n    'wander',\n    'lookAround',\n    'focusPulse',\n    'shake',\n];\nexports.bodyMotionModes = [\n    'none',\n    'slowDrift',\n    'breathe',\n    'bob',\n    'bounce',\n    'sway',\n    'float',\n    'shake',\n];\nconst eyeMotionSet = new Set(exports.eyeMotionModes);\nconst bodyMotionSet = new Set(exports.bodyMotionModes);\nconst isEyeMotion = (value) => typeof value === 'string' && eyeMotionSet.has(value);\nexports.isEyeMotion = isEyeMotion;\nconst isBodyMotion = (value) => typeof value === 'string' && bodyMotionSet.has(value);\nexports.isBodyMotion = isBodyMotion;\nconst smoothstep = (value) => value * value * (3 - 2 * value);\nconst hash = (value) => {\n    const raw = Math.sin(value * 127.1 + 311.7) * 43758.5453;\n    return (raw - Math.floor(raw)) * 2 - 1;\n};\nconst expressionSeed = (expression) => expression.headX * 0.71 + expression.headY * 1.13 + expression.headZ * 1.37;\nconst EYE_MOTION_SEED = 17.29;\nconst smoothNoise = (elapsedMs, axis, seed, interval) => {\n    const progress = elapsedMs / interval;\n    const step = Math.floor(progress);\n    const blend = smoothstep(progress - step);\n    const previous = hash(step * 3 + axis + seed);\n    const next = hash((step + 1) * 3 + axis + seed);\n    return previous + (next - previous) * blend;\n};\nconst saccade = (elapsedMs, axis, seed) => {\n    const interval = 1100;\n    const duration = 140;\n    if (elapsedMs <= 0)\n        return 0;\n    const step = Math.floor(elapsedMs / interval);\n    const progress = (elapsedMs - step * interval) / duration;\n    const blend = smoothstep(Math.min(progress, 1));\n    const previous = step === 0 ? 0 : hash((step - 1) * 2 + axis + seed);\n    const next = hash(step * 2 + axis + seed);\n    return previous + (next - previous) * blend;\n};\nconst hasAmbientMotion = (expression) => expression.eyeMotion !== 'none' || expression.bodyMotion !== 'none';\nexports.hasAmbientMotion = hasAmbientMotion;\nconst ambientBodyOffset = (expression, elapsedMs, strength = 1) => {\n    const seed = expressionSeed(expression);\n    if (expression.bodyMotion === 'slowDrift') {\n        return {\n            x: smoothNoise(elapsedMs, 3, seed, 2900) * 1.45 * strength,\n            y: smoothNoise(elapsedMs, 4, seed, 3700) * 1.1 * strength,\n        };\n    }\n    if (expression.bodyMotion === 'breathe') {\n        const time = elapsedMs / 1000;\n        return { x: 0, y: Math.sin(time * 1.7) * 1.15 * strength };\n    }\n    if (expression.bodyMotion === 'bob') {\n        const time = elapsedMs / 1000;\n        return { x: 0, y: Math.sin(time * 3.2) * 2.2 * strength };\n    }\n    if (expression.bodyMotion === 'bounce') {\n        const time = elapsedMs / 1000;\n        return {\n            x: Math.sin(time * 2.4) * 0.9 * strength,\n            y: -Math.abs(Math.sin(time * 3.6)) * 4.2 * strength,\n        };\n    }\n    if (expression.bodyMotion === 'sway') {\n        const time = elapsedMs / 1000;\n        return { x: Math.sin(time * 1.8) * 2.1 * strength, y: Math.cos(time * 1.4) * 0.55 * strength };\n    }\n    if (expression.bodyMotion === 'float') {\n        return {\n            x: smoothNoise(elapsedMs, 5, seed, 4200) * 2.2 * strength,\n            y: smoothNoise(elapsedMs, 6, seed, 5100) * 2.5 * strength,\n        };\n    }\n    if (expression.bodyMotion === 'shake') {\n        const time = elapsedMs / 1000;\n        return {\n            x: (Math.sin(time * 31) + Math.sin(time * 53) * 0.45) * 1.35 * strength,\n            y: (Math.sin(time * 37) + Math.sin(time * 61) * 0.4) * 1.1 * strength,\n        };\n    }\n    return { x: 0, y: 0 };\n};\nexports.ambientBodyOffset = ambientBodyOffset;\nconst ambientEyeOffset = (expression, elapsedMs, strength = 1) => {\n    if (expression.eyeMotion === 'microSaccades') {\n        return {\n            x: saccade(elapsedMs, 0, EYE_MOTION_SEED) * 1.5 * strength,\n            y: saccade(elapsedMs, 1, EYE_MOTION_SEED) * 0.9 * strength,\n        };\n    }\n    if (expression.eyeMotion === 'wander') {\n        return {\n            x: smoothNoise(elapsedMs, 9, EYE_MOTION_SEED, 2100) * 3.2 * strength,\n            y: smoothNoise(elapsedMs, 10, EYE_MOTION_SEED, 2700) * 1.8 * strength,\n        };\n    }\n    if (expression.eyeMotion === 'lookAround') {\n        const time = elapsedMs / 1000;\n        return {\n            x: Math.sin(time * 1.8) * 4.2 * strength,\n            y: Math.sin(time * 0.9 + 0.7) * 1.4 * strength,\n        };\n    }\n    if (expression.eyeMotion === 'focusPulse') {\n        const time = elapsedMs / 1000;\n        return { x: Math.sin(time * 5.2) * 0.35 * strength, y: Math.cos(time * 4.8) * 0.25 * strength };\n    }\n    if (expression.eyeMotion === 'shake') {\n        const time = elapsedMs / 1000;\n        return {\n            x: (Math.sin(time * 47) + Math.sin(time * 71) * 0.45) * 1.2 * strength,\n            y: (Math.sin(time * 59) + Math.sin(time * 83) * 0.4) * 0.8 * strength,\n        };\n    }\n    return { x: 0, y: 0 };\n};\nexports.ambientEyeOffset = ambientEyeOffset;\nconst applyAmbientBodyMotion = (expression, elapsedMs, strength = 1) => {\n    const next = { ...expression };\n    const seed = expressionSeed(expression);\n    if (expression.bodyMotion === 'slowDrift') {\n        next.headX += smoothNoise(elapsedMs, 0, seed, 2600) * 0.8 * strength;\n        next.headY += smoothNoise(elapsedMs, 1, seed, 3300) * 1.15 * strength;\n        next.headZ += smoothNoise(elapsedMs, 2, seed, 4100) * 0.45 * strength;\n    }\n    else if (expression.bodyMotion === 'breathe') {\n        const time = elapsedMs / 1000;\n        next.headX += Math.sin(time * 1.7) * 0.45 * strength;\n    }\n    else if (expression.bodyMotion === 'bob') {\n        const time = elapsedMs / 1000;\n        next.headX += Math.sin(time * 3.2) * 1.4 * strength;\n    }\n    else if (expression.bodyMotion === 'bounce') {\n        const time = elapsedMs / 1000;\n        next.headZ += Math.sin(time * 3.6) * 2.2 * strength;\n    }\n    else if (expression.bodyMotion === 'sway') {\n        const time = elapsedMs / 1000;\n        next.headZ += Math.sin(time * 1.8) * 3.4 * strength;\n        next.headY += Math.sin(time * 0.9) * 1.2 * strength;\n    }\n    else if (expression.bodyMotion === 'float') {\n        next.headX += smoothNoise(elapsedMs, 7, seed, 4200) * 1.25 * strength;\n        next.headY += smoothNoise(elapsedMs, 8, seed, 5100) * 1.8 * strength;\n        next.headZ += smoothNoise(elapsedMs, 9, seed, 4700) * 0.9 * strength;\n    }\n    else if (expression.bodyMotion === 'shake') {\n        const time = elapsedMs / 1000;\n        next.headX += (Math.sin(time * 31) + Math.sin(time * 53) * 0.45) * 1.15 * strength;\n        next.headY += (Math.sin(time * 37) + Math.sin(time * 61) * 0.4) * 1.35 * strength;\n        next.headZ += Math.sin(time * 43) * 0.7 * strength;\n    }\n    return next;\n};\nexports.applyAmbientBodyMotion = applyAmbientBodyMotion;\nconst applyAmbientMotion = (expression, elapsedMs, strength = 1) => {\n    const next = (0, exports.applyAmbientBodyMotion)(expression, elapsedMs, strength);\n    const eyeOffset = (0, exports.ambientEyeOffset)(expression, elapsedMs, strength);\n    next.positionXLeft += eyeOffset.x;\n    next.positionXRight += eyeOffset.x;\n    next.positionYLeft += eyeOffset.y;\n    next.positionYRight += eyeOffset.y;\n    return next;\n};\nexports.applyAmbientMotion = applyAmbientMotion;\n},\"/export/standaloneEngine.entry\":function(require,module,exports){\"use strict\";\nObject.defineProperty(exports, \"__esModule\", { value: true });\nexports.hasAmbientMotion = exports.applyAmbientMotion = exports.applyAmbientBodyMotion = exports.ambientEyeOffset = exports.ambientBodyOffset = exports.renderAvatar = exports.poseFromExpression = exports.expressionFields = void 0;\nvar geometry_1 = require(\"../avatar/geometry\");\nObject.defineProperty(exports, \"expressionFields\", { enumerable: true, get: function () { return geometry_1.expressionFields; } });\nObject.defineProperty(exports, \"poseFromExpression\", { enumerable: true, get: function () { return geometry_1.poseFromExpression; } });\nObject.defineProperty(exports, \"renderAvatar\", { enumerable: true, get: function () { return geometry_1.renderAvatar; } });\nvar ambientMotion_1 = require(\"../avatar/ambientMotion\");\nObject.defineProperty(exports, \"ambientBodyOffset\", { enumerable: true, get: function () { return ambientMotion_1.ambientBodyOffset; } });\nObject.defineProperty(exports, \"ambientEyeOffset\", { enumerable: true, get: function () { return ambientMotion_1.ambientEyeOffset; } });\nObject.defineProperty(exports, \"applyAmbientBodyMotion\", { enumerable: true, get: function () { return ambientMotion_1.applyAmbientBodyMotion; } });\nObject.defineProperty(exports, \"applyAmbientMotion\", { enumerable: true, get: function () { return ambientMotion_1.applyAmbientMotion; } });\nObject.defineProperty(exports, \"hasAmbientMotion\", { enumerable: true, get: function () { return ambientMotion_1.hasAmbientMotion; } });\n}},cache={};function resolve(from,request){if(request[0]!=='.')return request;var parts=from.split('/');parts.pop();request.split('/').forEach(function(part){if(!part||part==='.')return;if(part==='..')parts.pop();else parts.push(part)});return parts.join('/')||'/'}function load(id){if(cache[id])return cache[id].exports;var factory=modules[id];if(!factory)throw new Error('Standalone module not found: '+id);var module={exports:{}};cache[id]=module;factory(function(request){return load(resolve(id,request))},module,module.exports);return module.exports}return load('/export/standaloneEngine.entry')})();"
